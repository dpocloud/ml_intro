<!--

author:     Alexander Trofimov
attribute:  **Editor:** Daria Gudkova
email:    
date:       21/02/2025
version:    0.1
language:   en
narrator:   English

icon:    ./icon.png
logo:    ./course_logo.png

comment:  Курс "Введение в машинное обучение"

link:     ./custom.css

import:   ./macro.md

-->



## Обучение с учителем: основные принципы
-----

!?[Lecture02 Video](https://www.youtube.com/watch?v=8pTEmbeENF4)

В разделе рассматриваются ключевые аспекты обучения с учителем, включая основные концепции, методы и подходы к построению и оценке моделей машинного обучения. Вводятся понятия гипотезы и алгоритма обучения, который формирует гипотезу на основе данных, и обсуждается роль индуктивного порога в определении класса моделей. Вводятся понятие функции потерь как меры неточности модели, понятия теоретического и эмпирического риска модели, принцип минимизации эмпирического риска и рассматривается проблема оценивания обобщающей способности модели и переобучения.

Подробно обсуждается разложение ошибки модели машинного обучения на смещение (bias) и дисперсию (variance), а также их связь с сложностью модели и индуктивным порогом. Рассматриваются методы оценки точности моделей, включая кросс-валидацию (hold-out, k-fold, LOOCV), и важность стратификации выборки для сохранения баланса классов. Также затрагивается тема внутренней кросс-валидации, используемой для выбора гиперпараметров и оптимизации обобщающей способности модели.

---
<h3 style="text-align: center;">Содержание раздела</h3>

- Постановка задачи обучения с учителем

  - Признаки и отклики модели
  - Гипотеза и обучающийся алгоритм

- Индуктивный порог и генерализация

  - Понятие индуктивного порога
  - Генерализация, недообучение и переобучение
  - Проблема оценивания обобщающей способности модели

- Функция потерь и эмпирический риск

  - Теоретический и эмпирический риск
  - Принцип минимизации эмпирического риска
  - Смещение и дисперсия ошибки обучаемой модели. Разложение «bias-variance»
  - Связь сложности модели, индуктивного порога и способности к обобщению
  - Оценивание точности обученных моделей

- Методы кросс-валидации 

  - hold-out, k-fold, LOOCV
  - Стратификация выборки при кросс-валидации
  - Внутренняя кросс-валидация

-----


### Постановка задачи обучения с учителем
-----

**Обучение с учителем (Supervised Learning)** — метод машинного обучения, при котором модель обучается на данных, состоящих из входных объектов (признаков) и соответствующих им выходных значений (меток). 
Цель — построить модель, которая сможет предсказывать выходные значения для новых входных данных.

**Входные данные (Instance Domain)** $𝒳$ — множество объектов, которые описываются набором признаков.

**Выходные данные (Response Domain)** $𝒴$ — множество меток или откликов, соответствующих входным объектам.

**Пример задачи:** классификация фруктов на конвейере (яблоки и апельсины). На основе характеристик объекта (масса, размер, цвет) модель определяет, к какому классу относится объект.
@img02(classification1.jpg, Классификация фруктов, 50) 

----
Признаки
----

На вход модели нельзя подавать ничего кроме чисел, поэтому объект предстатвляется с помощью признаков. Если признак объекта изначально представляет собой что-либо кроме числа (изображение, текст и др.), то этот признак приводится к числовому значению (часто разделяясь на множество признаков). Способы приведения признаков к числовым значениям рассмотрены [в следующих главах](#Slide-Title). 

**Признаки (Features)** — это ~~числовые~~ характеристики объекта, которые подаются на вход модели. 

Когда мы говорим, что подаем объект $x \in 𝒳$ на вход модели, то подразумеваем под этим то, что мы подаем на вход **вектор признаков** объекта $x$: <br>
$$f(x) = (f_1(x), ..., f_M(x)),$$ где <br>
$f(x) ∈ D_1 × ... × D_M$, <br>
$f_j : 𝒳 → D_j$ -- $j$-й признак, <br>
$D_j$ — область определения $j$-го признака (**feature domain**), <br>
$j = 1,...,M$.

Вопросом о выборе оптимальных признаков занимается [Feature Engeneering](#Slide-Title).

**Типы признаков:**

- **Бинарные**: принимают два значения (0 или 1) <br> $D_j = \{0, 1\}$
- **Номинальные (категориальные)**: принимают конечное число значений <br> $|D_j| < ∞$
- **Ординальные**: упорядоченные категории <br> $|D_j| < ∞, где \ D_j$ упорядочено
- **Действительные**: непрерывные числовые значения <br> $D_j ⊆ \mathbb{R}$

@quizQuestion(Сопоставьте следующие признаки с их типами:)

<!-- data-randomize -->
- [(Бинарный) (Номинальный) (Ординальный) (Действительный)]
- [ (x) ( ) ( ) ( )]  Пол человека
- [ ( ) (x) ( ) ( )]  Цвет глаз
- [ ( ) ( ) (x) ( )]  Оценки ("удовлетворительно", "хорошо", "отлично")
- [ ( ) ( ) ( ) (x)]  Вес

----
Задачи обучения с учителем
----

Тип задачи обучения с учителем определяется типом выхода.

- **Регрессия**: выходные значения непрерывны <br> $𝒴 = \mathbb{R}\ или\ 𝒴 = \mathbb{R}^L$
- **Классификация**: выходные значения дискретны <br> $𝒴 = \{−1, 1\}$ или $𝒴 = \{0, 1\}$ — бинарная классификация <br> $𝒴 = \{1, ..., K\}$ — многоклассовая классификация

@img02(regr.png, Типы откликов, 50)

@quizQuestion(`Сопоставьте следующие примеры откликов/меток с типами задач:`)

<!-- data-randomize -->
- [(Регрессия) (Бинарная классификация) (Многоклассовая классификация)]
- [ ( ) (x) ( )]  Определение, является ли электронное письмо спамом или не спамом
- [ ( ) ( ) (x)]  Определение типа цветка
- [ (x) ( ) ( )]  Прогнозирование стоимости дома

----
Постановка задачи обучения с учителем
----

$𝒳$ — множество объектов

$𝒴$ — множество откликов

$F : 𝒳 → 𝒴$ — неизвестное моделируемое отображение

**Дано:**

$D = \{(x^{(1)}, y^{(1)}), ...,(x^{(n)}, y^{(n)})\}$ — имеющаяся выборка

$x^{(1)}, ..., x^{(n)} ∈ 𝒳$ — выборка объектов

$y^{(1)}, ..., y^{(n)} ∈ 𝒴$ — выборка откликов

$y^{(i)} = F(x^{(i)}),\ i = \overline{1,n}$

**Найти:**

$h : 𝒳 → 𝒴$ -- оценка отображения $F$ (**гипотеза**)

@quizQuestion(`Почему мы ищем не само отображение $F$, а его оценку?`)

<!-- data-randomize -->
[( )] Функция всегда определена на конечном множестве точек.
[(x)] По дискретному набору точек восстановить функцию невозможно, т.к. функция - это непрерывная зависимость.
[( )] Оценка функции всегда точнее самой функции.
[( )] Дискретные точки всегда содержат всю информацию о функции.

Возникают следующие вопросы: 

- Как понять, насколько полученная гипотеза $h$ близка к истинной ~~неизвестной~~ зависимости $f$. Иными словами, как понять насколько хороша полученная оценка?
- Как найти гипотезу $h$?

----
Алгоритм обучения
----

**Алгоритм обучения** $μ$ — это отображение произвольной выборки данных $D ∈ (𝒳 × 𝒴 )^n$ в гипотезу $h ∈ ℋ$:
$$μ : (𝒳 × 𝒴 )^n → ℋ ,$$
где $ℋ$ — множество гипотез $𝒳 → 𝒴$ (hypothesis domain)

@img02(LearningAlgorithm.png, Алгоритм обучения и гипотеза, 75)

Функция $μ$ принимает на вход обучающую выборку $D$ и выдает на выходе некоторую гипотезу $h$. На эту гипотезу $h$ мы можем подать любой $x$ и получить соответствующий $y$.

@NB(`~~Важно.~~ Гипотеза $h$ не может быть любой, она должна принадлежать классу гипотез $ℋ$. Мы ограничиваем алгоритм обучения так, чтобы на выходе он мог выдать только функцию $h$, которая принадлежит заданному классу функций (т.е. находимся в некотором функциональном пространстве).`)

Примеры доменов $ℋ$:

- $ℋ$ может быть классом линейных функций, тогда, подав на вход элементы обучающей выборки, на выходе гипотеза выдаст линейную комбинацию признаков:
$$ℋ=\{ h:h(x)=\sum_{j=1}^M{\beta_j f_j(x)} \},\ 𝒴=\mathbb{R}$$

- В задаче бинарной классификации гипотеза определяется знаком линейной комбинации признаков:
$$ℋ=\{ h:h(x)=sign\sum_{j=1}^M{\beta_j f_j(x)} \},\ 𝒴=\{-1,1\}$$

@quizQuestion(В чем состоит цель алгоритма обучения?)

**Цель алгоритма обучения**: построить гипотезу $h$, которая аппроксимирует неизвестную функцию $f$, [[ (в обучающих точках) | в точках, которых не было в обучащей выборке]].

Гипотеза $h$ должна быть ~~индуктивной~~, то есть обучившись на частных примерах, которые были в обучающей выборке, она должна обобщить закономерности, которые присутствовали в этой выборке, на более широкую область входных примеров. Напомним, что индукция, – это умозаключение от частного к общему.

Однако, модель не обязана хорошо работать на тех примерах, которых не было в обучающей выборке.
Мы можем на это только надеяться, и проверить это постфактум. Проблема в том, что такое требование в процессе обучения мы заложить не можем, потому что сама задача моделирования неизвестной функции $f$ на основе конечного числа обучающих примеров, является **плохо обусловленной (ill-posed)**, т.е. имеет бесконечно много решений. Через конечный набор обучающих точек можно провести бесконечно много функций.

Как на выходе обучающегося алгоритма получить ~~единственное~~ решение, максимально близкое к неизвестной функции $f$?
Для этого перед построением гипотезы можно выдвинуть некоторые дополнительные предположения относительно того, какую функцию гипотеза $h$ должна реализовывать. Такие дополнительные предположения мы будем называть [индуктивным порогом (inductive bias)](#15).

### Индуктивный порог и генерализация

----
Индуктивный порог
----

**Индуктивный порог (inductive bias)** алгоритма обучения — это множество предположений, которые позволяют сделать плохо обусловленную задачу машинного обучения корректной.

**Замечание:**

- inductive bias $\neq$ statistical bias

  - statistical bias -- числовое значение
  - inductive bias -- множество предположений

Индуктивный порог определяется классом гипотез $ℋ$ , которые могут быть получены в результате работы алгоритма обучения.
Мы задаем класс моделей, но позволяем данным выбрать модель из этого класса.

| Слишком слабый индуктивный порог | Слишком сильный индуктивный порог |
| -------- | ------- |
| Слабые предположения о классе моделей $ℋ$ | Сильные предположения о классе моделей $ℋ$ |
| Широкий класс моделей | Отсутствие гибкости |
| Сильная чувствительность к данным | Игнорирование данных |
| Много степеней свободы | Мало степеней свободы |

**Примеры индуктивных порогов.**

- В классификации: близкие примеры, как правило, принадлежат одному и тому же классу, далекие примеры принадлежат разным классам.
- В регрессии: функция регрессии линейна.

Чтобы измерить качество индуктивного порога, введем понятие ~~генерализации~~, или обобщающей способности модели. 

----
Генерализация, недообучение и переобучение
----

**Генерализация (обобщающая способность модели)** — это способность модели точно предсказывать отклики на новых данных.

**Недообучение (underfitting)**: модель не способна моделировать закономерности и паттерны в данных.

Модель не способна показывать хорошую точность на примерах обучающей выборки, не говоря уже о новых примерах:

- Высокая ошибка на обучающих данных
- Высокая ошибка на тестовых данных
- Причиной часто является избыточное упрощение гипотез (например, использование линейной модели для аппроксимации нелинейной зависимости).

**Переобучение (overfitting)**: модель настроена на моделирование шумов вместо истинного отображения $F$.

Модель хорошо воспроизводит примеры обучающей выборки, но при этом плохо воспроизводит примеры, которых не было в обучающей выборке:

- Низкая ошибка на обучающих данных.
- Высокая ошибка на тестовых данных
- Обусловлено избыточной сложностью модели (напр., полином высокой степени для данных с простой структурой).

**Способность к генерализации зависит от индуктивного порога.**

| Слишком строгий порог | Оптимальный порог | Слишком слабый порог |
|---|---|---|
| Недообучение:<br>модель не может аппроксимировать истинную функцию. | Оптимальная модель:<br>минимизация ошибки на обучающих данных при сохранении обобщающей способности. | Переобучение:<br>модель адаптируется к шуму. |
| @img02(degree1.png, Полином 1 степени, 75) | @img02(degree4.png, Полином 4 степени, 75) | @img02(degree15.png, Полином 15 степени, 75) |
| Для истинной нелинейной зависимости F выбор линейной гипотезы приводит к систематическому смещению. Большая ошибка как на обучающих точках, так и на других примерах. | Построенная модель хорошо работает как на обучающих примерах, так и на данных, которых не было в обучающей выборке. | Выбрали полином слишком большой степени, позволив модели быть гораздо более сложной, чем моделируемая зависимость. Модель идеально воспроизводит конкретные обучающие примеры, но если пример отличается от обучающих, то ошибка сразу становится большой. |

@NB(`~~Важно.~~ Модель должна демонстрировать высокую точность на новых данных, а не на обучающих. Даже если полином низкой степени показывает худшие результаты на обучающей выборке по сравнению с полиномом высокой степени, это не означает, что он будет работать хуже и на новых данных.`)

----
Оценивание способности к обобщению
----

Для измерения обобщающей способности необходимы данные, которые модель не использовала в процессе обучения.

**Типы выборок данных:**

- **Исходные данные $D$**:

  - Это выборка, на основе которой строится модель.
  - Разделяется на две части: обучающую и тестовую выборки.

- **Обучающая выборка $D_{Tr}$**:

  - Основная часть данных, используемая для настройки параметров модели.
  - Внутри обучающей выборки выделяются две подвыборки:

    - **Обучающая подвыборка $D_T$**: используется для минимизации ошибки модели.
    - **Валидационная подвыборка $D_V$**: используется для оценки обобщающей способности модели в процессе обучения.

- **Тестовая выборка $D_{Tst}$**:

   - Используется для финальной оценки качества модели.
   - Модель не должна видеть эти данные в процессе обучения.

$$D=(D_T \cup D_V) \cup D_{Tst}$$
$$D_{Tr}=D_T \cup D_V$$

@img02(validation.png, Разбиение на выборки, 75)

**Роль валидационной выборки**:

- Валидационная выборка помогает выбрать наилучшую модель из множества возможных.
- Валидационная выборка не является обязательной, если строится только одна модель. Однако она необходима для оптимизации обобщающей способности при выборе среди нескольких моделей.
- Если несколько моделей показывают схожую точность на обучающей выборке, то лучшей считается та, которая демонстрирует наилучшие результаты на валидационной выборке.

**Роль тестовой выборки**:

- Тестовая выборка используется только один раз для окончательной оценки качества модели.
- Модель не должна подгоняться под тестовые данные, так как это приведёт к необъективной оценке.
- Тестовые данные должны быть полностью независимыми и не должны использоваться в процессе обучения или валидации.

@quizQuestion(Сравните валидацию и тестирование:)

|Валидация|Тестирование|
|---|---|
| [[(Происходит во время обучения.)|Происходит после завершения обучения.]]| [[Происходит во время обучения.|(Происходит после завершения обучения.)]]|
| [[(Цель — оптимизация обобщающей способности модели.)|Цель — окончательная оценка качества модели.]] |[[Цель — оптимизация обобщающей способности модели.|(Цель — окончательная оценка качества модели.)]]|
| [[(Данные используются для настройки модели.)|Данные не должны использоваться в процессе обучения или валидации.]] | [[Данные используются для настройки модели.|(Данные не должны использоваться в процессе обучения или валидации.)]]|


### Функция потерь и эмпирический риск

----
Функция потерь
----

Подавая на вход модели $h$ значение $x$, мы ожидаем что значение выхода модели $h(x)$ должно быть максимально близким к значению выхода неизвестной зависимости $F$. Напомним, что в [Data-Driven подходе](#15) мы требуем от модели не повторения структуры или механизма функции $F$, а лишь внешнего схожего поведения, рассматривая функцию $F$ как "черный ящик", который слишком сложен для описания формулами.

Для измерения того, насколько выход модели отличается от выхода объекта, необходима числовая мера. Эта мера называется **функцией потерь**. Функция потерь может быть любой функцией, которая принимает на вход: наблюдаемое значение $y$ и выход модели $h(x)$. Область значений функции потерь – неотрицательные числа, и чем больше значение функции потерь, тем хуже модель. В идеальном случае (если выход модели совпадает с выходом объекта) функция потерь должна быть равна нулю.

@img02(LossFunction.png, Функция потерь, 60)

**Функцией потерь (loss function, cost function)** $L(h,(x, y)) ∈ \mathbb{R}^+$ называется некоторая мера неточности модели $h \in ℋ$ на примере $(x, y) \in 𝒳 ×𝒴$.

При сравнении качества нескольких моделей, **меньшие потери означают лучшую модель**, $L(h,(x, y)) = 0$ — отсутствие ошибки для $x \in 𝒳$.

----
Статистический взгляд
----

Рассмотрим процедуру обучения с точки зрения статистики. Сложность моделирования неизвестного отображения $F: 𝒳 → 𝒴$ заключается в том, что это отображение обычно ~~не является детерминированным~~: при подаче одного и того же значения $x$ на вход объекта, мы можем получать разные значения $y$ на выходе. Это может происходить по следующим причинам.

- Внутри моделируемого объекта есть стохастические процессы, которые мы не можем контролировать (например, погрешность измерений).
- Моделируемый объект детерминирован, но мы не учитываем все факторы, влияющие на $y$, что создает эффект случайности.

  - Например, если бросить монету 🪙, результат будет казаться случайным, так как мы не знаем, выпадет ли орел или решка. Однако, если бы мы знали силу броска, вращающий момент и другие факторы, такие как сопротивление воздуха или высота, мы могли бы построить модель, которая предсказывает результат с высокой точностью. В этом случае объект перестает быть случайным. 
  - В реальных задачах, таких как прогнозирование курса доллара 📈, сложно учесть все факторы, влияющие на результат. Может быть, курс зависит от предыдущих значений, цен на другие товары или даже от фазы Луны. Поскольку состав этих факторов неизвестен, выход моделируемого объекта кажется случайным.

Таким образом, на выходе моделируемого объекта $F$ наблюдается ~~случайная~~ величина $y$.

Заметим, что модель, которая получается в результате работы алгоритма машинного обучения, всегда ~~детерминирована~~. Мы знаем формулу, по которой $x$ преобразуется в выход модели $h(x)$.

- Пусть $y$ — наблюдение случайной величины $Y$ с условным распределением $f_Y (y|x)$ для заданного $x ∈ 𝒳$.
- Пусть $x \in 𝒳$ — это наблюдение случайного вектора $X$, имеющего распределение $f_X(x)$.

Тогда значение функции потерь $L(h,(X, Y))$ — это ~~случайная величина~~ (как функция случайного вектора $(X, Y)$).

$D = \{(x^{(1)}, y^{(1)}), ...,(x^{(n)}, y^{(n)})\}$ — выборка наблюдений случайного вектора $(X, Y)$, полученная из распределения $f_{XY}(x, y) = f_Y(y|x)f_X(x)$.

**Замечание.** Выборка данных всегда рассматривается как совокупность наблюдений некоторой случайной величины. Это является стандартным подходом как в математической статистике, так и в машинном обучении.

На практике, $f_{XY}(x, y)$ обычно ~~неизвестна~~, и для построения модели используется только выборка $D$.

Иными словами, выборка $D$ предоставляется без какой-либо информации о лежащем в ее основе распределении. Нам доступна лишь сама выборка, а также поставленная задача, например, построение алгоритма классификации или регрессионной модели.

----
Риск и эмпирический риск
----

**Риском** $R(h)$, связанным с моделью $h$, называется математическое ожидание функции потерь. 

Функция потерь $L$ является случайной величиной, так как она зависит от других случайных величин $x$ и $y$. Согласно теории вероятностей, математическое ожидание функции случайных величин вычисляется как интеграл от функции потерь по совместной плотности распределения $f(x,y)$ в пространстве $𝒳×𝒴$:

$$R(h)=M[L(h, (𝒳, 𝒴))] = \int_{𝒳×𝒴}L(h, (𝒳, 𝒴))f_{XY}(x, y)dxdy$$

Функция потерь $L$ задается нами, однако совместная плотность $f(x,y)$ неизвестна => точно рассчитать теоретический риск модели невозможно. Вместо этого мы можем оценить риск модели по выборке $D$, учитывая, что наиболее эффективной оценкой математического ожидания является среднее арифметическое.

**Эмпирическим риском** $R^*(h)$, связанным с моделью $h$, называется оценка риска $R(h)$ как среднее значение функции потерь по выборке $D$:
$$R^*(h)=\frac{1}{n}\sum_{i=1}^{n}{L(h, (x^{(i)}, y^{(i)}))}$$

Другими словами, **эмпирический риск** $R^*(h)$ – это средняя ошибка модели $h$ на выборке $D$.

Эмпирический риск может быть выражен по-разному в зависимости от выбранной функции потерь.

- **Пример для регрессии:**

$L(h,(x, y)) = (h(x) − y)^2$ – квадратичная функция потерь.

$R^*(h) = \frac{1}{n}\sum_{i=1}^{n} (︀h(x^{(i)}) − y{(i)})︀^2$ – mean squared error (MSE).

- **Пример для классификации:**

$L(h,(x, y)) = [h(x) \neq y]$ – 0-1 функция потерь.

$R^*(h) = \frac{1}{n}\sum_{i=1}^{n} [h(x^{(i)}) \neq y^{(i)}]$ – ошибка классификации (доля неправильно классифицированных примеров в выборке).

Обучение модели сводится к задаче оптимизации, где из множества гипотез $ℋ$ выбирается модель $h$, минимизирующая эмпирический риск. 
Поэтому машинное обучение тесно связано с методами оптимизации, в число которых входит [градиентный спуск](#15).

**Целевая функция алгоритма обучения $μ$:**

$R^*_T(h) \rightarrow \min_{h \in ℋ}$ – минимизация эмпирического риска (empirical risk minimization, ERM).

Обучение модели по принципу ERM состоит в решении оптимизационной задачи.

----
Риск с квадратичной функцией потерь
----

Рассмотрим частный случай, когда функция потерь является квадратичной. Квадратичная функция потерь часто используется в задачах регрессии, благодаря своей простоте и дифференцируемости. 

$h(x)$ — отклик модели $h$ для $x \in 𝒳$ (детерминирован)

$Y = F(x)$ — значение моделируемой функции для $x \in 𝒳$ (случайная величина)

Риск с квадратичной функцией потерь $L(h,(x, Y))$ для $x \in 𝒳$ :

$r(h,x) = M[L(h, (x, Y))|x] = M[(h(x)-Y)^2|x]=$

$=h^2(x)-2h(x)M[Y|x]+\textcolor{blue}{M[Y^2|x]}=$

$=h^2(x)-2h(x)M[Y|x]+D[Y|x]+(M[Y|x])^2=$

$=(h(x)-M[Y|x])^2+\sigma^2_x$

$$r(h,x) = (h(x)-M[Y|x])^2+\sigma^2_x$$
где:

$(h(x)-M[Y|x])^2$ – **ошибка модели** $h$ для $x ∈ 𝒳$

$\sigma^2_x=D[Y|x]$ – **шум**, не зависит от $D$ и $h$

Таким образом, риск модели разлагается на две составляющие: ошибку модели и шум (условную дисперсию случайной величины $Y$ при $x$). 

Что можно сделать для минимизации риска?

- На шум повлиять невозможно, т.к. дисперсия $Y$ не зависит от модели.
- Можно минимизировать ошибку модели, сделав так, чтобы выход модели $h(x)$ совпадал с условным математическим ожиданием моделируемой функции $M[Y|x]$. Иными словами, ~~модель должна воспроизводить функцию регрессии y на x.~~

$h(x)=M[Y|x],\ \forall x \in 𝒳 \Leftrightarrow \ h(x)$ – **функция регрессии $y$ на $x$**

@NB(`~~Важно.~~ В реальных данных для одного $x$ обычно есть только одно значение $y$, и модель не должна стремиться точно воспроизводить это значение. Вместо этого она должна предсказывать математическое ожидание $M[Y|x]$, т.е. функцию регрессии в точке $x$. Модель минимизирует ошибку, но не может повлиять на дисперсию данных.`)

@quizQuestion(Что означает оптимальная модель обучения с учителем?)

- [( )] Модель будет оптимальной, если для каждого $x$ выход модели будет максимально близок к наблюдаемым значениям $y$.
- [(x)] Выход модели для данного $x$ должен быть равен значению функции регрессии в этой точке $M[Y|x]$.

**Что делать, если ошибка на обучающей выборке велика?**

❌ Неправильная стратегия:

Усложнение модели, например, переход к нейронным сетям или ансамблевым методам. В результате ошибка на обучающей выборке сводится к нулю, но модель начинает воспроизводить не функцию регрессии, а конкретные данные, включая шум.

Эмпирический риск (ошибка на обучающей выборке) может быть сведён к нулю, но истинный риск модели остаётся высоким. Это происходит из-за того, что модель начинает моделировать шум, а не функцию регрессии. В результате эмпирический риск становится смещённой оценкой, а истинный риск модели не уменьшается.

✅ Правильная стратегия:

Поиск причины:

- Либо модель действительно плохо воспроизводит функцию регрессии.
- Либо ошибка связана с высокой дисперсией данных, на которую модель повлиять не может. В таком случае не следует подгонять модель под данные, так как это увеличивает истинный риск. Если дисперсия данных велика, то, возможно, нужно:

  - Расширить набор признаков, чтобы лучше детерминировать $y$.
  - Выбрать другой класс моделей.
  - Настроить гиперпараметры обучения.

**Принцип GIGO "garbage in, garbage out"**:

- Качество результатов работы любой аналитической модели полностью зависит от качества данных, на которых она построена.
- Если качество данных низкое (они содержат много шума), то и результаты будут соответствующими даже в том случае, если сам по себе алгоритм обучения модели полностью корректен.

@img02(GIGO.png, GIGO, 30)

----
Bias-Variance Decomposition
----

Итак, риск модели при фиксированном $x \in 𝒳$ состоит из двух слагаемых: ошибки и шума.

$$r(h, x)=(h(x)-M[Y|x])^2+\sigma^2_x$$

Ранее выход модели $h$ рассматривался как детерминированная величина $h(x)$. С другой стороны, гипотеза $h$ получается в результате работы алгоритма обучения $μ$, который зависит от ~~случайной~~ обучающей выборки $D_T$. Поскольку вход алгоритма зависит от $D_T$, то и его выход зависит от $D_T$. С этой точки зрения, выход модели $h$ при $x \in 𝒳$, обученной на случайной обучающей выборке $D_T$, является случайной величиной $h(x, D_T)$. 

💡**Замечание.** Из теории вероятностей известно, что повторное последовательное применение операторов матожидания эквивалентно одному единственному применению, но в пространстве объединенных плотностей. Поэтому, чтобы рассчитать риск модели в точке $x$, мы можем взять повторное мат. ожидание: сначала связанное с $Y$, затем связанное с $h$.

Математическое ожидание по всем случайным обучающим выборкам $D_T$:

$M[(h(x, D_T) - M[Y|x])^2]=$

$= \textcolor{blue}{M[h(x, D_T)^2]} - 2M[h(x, D_T)]M[Y|x]+M[Y|x]^2=$

$=D[h(x, D_T)]+M[h(x, D_T)]^2-2M[h(x, D_T)]M[Y|x]+M[Y|x]^2=$

$=(M[h(x, D_T)]-M[Y|x])^2+D[h(x, D_T)]$

где:

$(M[h(x, D_T)]-M[Y|x])$ – **смещение** модели $h$ для $x \in 𝒳$

$D[h(x, D_T)]$ – **дисперсия** модели $h$ по обучающим выборкам $D_T$

**Риск** модели $h$ при $x \in 𝒳$ (мат. ожидание функции потерь по обучающим выборкам $D_T$ ):

$$R(h, x)=(M[h(x, D_T)]-M[Y|x])^2+D[h(x, D_T)]+\sigma^2_x$$

$$R(h, x)=Bias^2[h]+D[h]+\sigma^2_x$$

**Источники ошибок модели:**

- **Смещение модели $Bias^2[h]$** 

  - Возникает вследствие неправильных предположений (плохой индуктивный порог). Например, при попытке с помощью линейной функции замоделировать полином четвертой степени, модель будет давать высокую ошибку независимо от того, на какой подвыборке данных, лежащих на этом полиноме четвертой степени, она была обучена.
  - Показывает, насколько мат. ожидание выходов моделей, обученных на всевозможных обучающих выборках, отклоняется от функции регрессии в точке $x$:<br>$Bias^2[h]= (M[h(x, D_T)]-M[Y|x])^2$

- **Дисперсия модели $D[h]$**

  - Ошибка вследствие дисперсии модели (неспособности правильной оценки параметров модели по ограниченным обучающим данным), т.е. неустойчивость модели.
  - Возникает вследствие дисперсии выходов моделей при фиксированном $x$. Обучающий алгоритм дает на выходе такие модели, которые при одном и том же $x$ дают существенно разные $y$. Это может быть связано со слабым индуктивным порогом, который приводит к высокой чувствительности к конкретным обучающим данным. 

    - Причем отдельно взятая модель, она может быть и довольно точной. Но риск -- это матожидание по всевозможным обучающим выборкам, поэтому дисперсия модели вносит вклад в ошибку модели.

- **Шум данных $\sigma^2_x$**

  - Неустранимая ошибка (не зависит от модели), которая может быть связана с:

    - качеством выбранной системы признаков: их достаточности, информативности;
    - природой самого объекта: насколько он шумный; 
    - особенностями сбора данных.

----
Bias-Variance Trade-off
----

Индуктивный порог определяет соотношение между смещением и дисперсией (**bias-variance trade-off**) модели $h$.

- Высокая дисперсия приводит к переобучению.
- Высокое смещение связано с недообучением.

**Сильный индуктивный порог**: низкое или высокое смещение, низкая дисперсия.

**Слабый индуктивный порог**: низкое или высокое смещение, высокая дисперсия.

**При правильном индуктивном пороге — низкое смещение и низкая дисперсия.**

----
📋 **Пример 1.** Пусть данные распределены вдоль параболы, как показано на рисунке. 

@img02(bv_all.png, Bias-Variance Trade-off, 80)

1. Допустим, мы выбрали слишком жесткий индуктивный порог, ограничившись классом линейных функций. Это означает, что независимо от того, какую случайную подвыборку данных мы возьмем (например, 80% точек), построенная регрессионная модель будет представлять собой прямую линию. Если мы возьмем другую подвыборку из 80% данных, результат будет аналогичным. Таким образом, независимо от выбора обучающей выборки, модель будет оставаться примерно одинаковой. Это указывает на низкую дисперсию (variance) модели, то есть вариабельность предсказаний при фиксированном $x$ будет минимальной. Однако такая модель будет иметь высокое смещение (bias), так как среднее значение предсказаний будет значительно отклоняться от истинной функции регрессии. В данном случае основной вклад в риск модели будет обусловлен смещением.

@img02(bv2.svg, Underfitting, 30)

2. Теперь рассмотрим альтернативный сценарий. Предположим, мы строим модель в виде ломаной линии, которая проходит через все точки обучающей выборки. Такая модель будет иметь нулевую ошибку на обучающих данных, так как она идеально соответствует всем точкам. Однако будет ли такая модель эффективной? Нет, так как риск модели останется высоким из-за высокой дисперсии. Если мы возьмем другую подвыборку данных и построим модель, то получим другую ломаную линию, так как некоторые точки не войдут в выборку. Если мы повторим этот процесс многократно и усредним результаты, то получим параболу, близкую к истинной функции регрессии. В этом случае смещение будет близким к нулю, но дисперсия будет высокой. Например, для фиксированного $x$ предсказания модели будут значительно варьироваться в зависимости от обучающей выборки. Таким образом, риск модели будет обусловлен высокой дисперсией, несмотря на то, что модель идеально соответствует обучающим данным.

@img02(bv2_1.svg, Overfitting, 30)

3. Если же мы выберем индуктивный порог, соответствующий полиному второй степени, и будем многократно обучать модель, то в среднем получим параболу, близкую к истинной функции регрессии. В этом случае и смещение, и дисперсия будут низкими, что является оптимальным сценарием. Даже если остаточная дисперсия (шум в данных) будет значительной, мы должны остаться в классе парабол, так как истинная функция регрессии является параболой. Точность модели может быть ограничена шумом в данных, который невозможно устранить, так как его моделирование приведет к увеличению риска.

@img02(bv2_3.svg, Good balance, 30)

----

📋 **Пример 2.** Аналогию bias и variance можно представить в виде стрельбы по мишени. Истинная модель соответствует яблочку мишени. Каждое обучение модели на конкретной выборке аналогично выстрелу.

- Если модель имеет высокое смещение и низкую дисперсию, это означает, что выстрелы постоянно отклоняются от цели, но попадают в одну и ту же точку. Это случай underfitting.
- Если модель имеет низкое смещение и высокую дисперсию, это означает, что в среднем выстрелы близки к цели, но отдельные выстрелы могут значительно отклоняться. Это случай overfitting. 
- Идеальный сценарий – низкое смещение и низкая дисперсия, когда все выстрелы постоянно попадают в цель.

@img02(bv.png, Darts, 50)

----
Индуктивный порог и сложность модели
----

Смещение и дисперсия связаны с индуктивным порогом, который определяется сложностью модели. Чем сложнее модель (больше параметров), тем слабее индуктивный порог, что приводит к низкому смещению, но высокой дисперсии (склонность к переобучению). Если модель простая (например, линейная), то смещение будет высоким, а дисперсия – низкой. Риск модели зависит от сложности, и существует оптимальный уровень сложности, минимизирующий общий риск.

- Сильный индуктивный порог -> низкая сложность модели
- Слабый индуктивный порог -> высокая сложность модели

@img02(biasvariance.png, Индуктивный порог и сложность модели, 60)

----
Генерализация и сложность модели
----

Способность модели к обобщению зависит от ее сложности.

На практике оптимальную сложность модели можно определить экспериментально, например, перебирая различные степени полиномов и выбирая ту, которая обеспечивает наименьшую ошибку на валидационной выборке.

@img02(biasvariance1.jpg, Генерализация и сложность модели, 70)

### Методы кросс-валидации

----
Оценивание ошибок модели
----

$D = (D_T \cup D_V ) \cup D_{Tst}$ — имеющиеся данные

$h \in ℋ$ — модель, обученная на $D_T$

$R(h)$ – истинный риск модели $h$

$R^*_T(h),R^*_V(h),R^*_{Tst}(h)$ — эмпирические риски (например, MSE) на обучающей, валидационной и тестовой выборках.

Истинный риск модели $R(h)$, который представляет собой математическое ожидание функции потерь по совместной функции плотности $f(x,y)$, невозможно рассчитать напрямую, так как эта функция плотности неизвестна. Однако истинный риск модели характеризует ее реальную ошибку: чем меньше истинный риск, тем точнее модель. Мы не можем вычислить истинный риск, мы можем лишь оценить его. Для этого используются различные выборки: обучающая, валидационная и тестовая.

Эмпирический риск (ошибка) на обучающей выборке $R^*_T(h)$ стремится к нулю, так как алгоритм обучения подбирает гипотезу $h$, минимизирующую ошибку на обучающих данных. Это делает оценку риска на обучающей выборке смещенной в сторону нуля, даже если истинный риск модели ненулевой.

Эмпирический риск (ошибка) на валидационной выборке $R^*_V(h)$ кажется более объективной оценкой риска, так как модель не видела эти данные в процессе обучения. Однако, если валидационная выборка используется для выбора наилучшей модели (например, модели с минимальной ошибкой на валидационных данных), то оценка риска снова становится смещенной в сторону нуля.

Наиболее объективной оценкой истинного риска является эмпирический риск (ошибка) модели на тестовой выборке $R^*_{Tst}(h)$. Однако проблема заключается в том, что эта оценка основывается на единственном значении ошибки, которое может быть случайным. Для повышения надежности оценки необходимо иметь несколько значений ошибки на тестовых выборках, чтобы можно было провести статистический анализ, например, оценить дисперсию или распределение ошибок.

Для этого используется метод многократного разбиения данных на обучающую и тестовую выборки. 
Исходный датасет разбивается на обучающую и тестовую части, модель обучается на обучающей выборке, а ошибка вычисляется на тестовой. Этот процесс повторяется многократно, что позволяет получить набор значений ошибок на тестовых выборках. На основе этих значений можно рассчитать среднее значение, дисперсию и другие статистические характеристики. Этот процесс называется **кросс-валидацией** и используется для оценки качества алгоритма машинного обучения, а не конкретной гипотезы.

@img02(cross1.png, Crosvalidation, 50)

$h_i$ — модель, обученная на обучающей выборке из $i$-го разбиения, $i = \overline{1,k}$ 

$R^*_{Tst}(h_1),...,R^*_{Tst}(h_k)$ — оценки риска моделей $h1, ..., hk$.

----
Методы кросс-валидации
----

**Определение.** <span style="color:darkred">Кросс-валидация (Cross-validation, CV)</span> — это методика оценивания качества алгоритма обучения, состоящая в оценивании способности моделей машинного обучения правильно работать на новых данных, не участвовавших в обучении.

Выбор метода кросс-валидации зависит от объема данных, сложности модели и вычислительных ресурсов.

- **Метод Монте-Карло (Repeated Random Sub-sampling CV)**

@img02(cv_rand.png, Repeated Random Sub-sampling CV, 50)

Данные случайным образом разбиваются на обучающую и тестовую выборки в заданной пропорции (например, 80% на обучение, 20% на тестирование). Процесс повторяется $k$ раз, что позволяет получить $k$ значений ошибки на тестовых выборках. 

Выборка ошибок: $R^*_{Tst}(h_1),...,R^*_{Tst}(h_k)$

Недостаток метода заключается в том, что некоторые примеры могут никогда не попасть в тестовую выборку.

- **k-fold CV**

@img02(cv_kfold.png, k-fold CV, 50)

Данные случайным образом разбиваются на $k$ равных выборок (folds). Одна из $k$ выборок используется для тестирования, оставшиеся $k-1$ выборок -- для обучения.

Выборка ошибок: $R^*_{Tst}(h_1),...,R^*_{Tst}(h_k)$

Этот метод гарантирует, что каждый пример попадет в тестовую выборку ровно один раз.

Недостаток – необходимость обучать модель $k$ раз, что может быть затратно при большом объеме данных или сложных моделях.

- **Leave-one-out (LOO) CV**

@img02(cv_loo.png, LOO CV, 50)

LOO CV — частный случай k-fold CV, где 
$k$ равно количеству примеров в выборке $k=n$. На каждой итерации один пример используется для тестирования, а остальные – для обучения. 

Выборка ошибок: $R^*_{Tst}(h_1),...,R^*_{Tst}(h_k)$

Этот метод особенно полезен при малом объеме данных, но требует значительных вычислительных ресурсов.

- **Holdout CV**

@img02(cv_holdout.png, Holdout CV, 50)

Данные случайным образом разбиваются на две выборки: обучающую и тестовую — в заданной пропорции, только один раз.

Выборка ошибок: $R^*_{Tst}(h)$

Этот метод прост и быстр, но предоставляет только одно значение ошибки. Он подходит для предварительных исследований, но не для окончательной оценки модели, так как он не дает возможность оценить дисперсию ошибок модели.

- **Resubstitution**

Обучающая выборка используется для тестирования.

----
Стратификация при разбиении
----

Рассмотрим пример задачи классификации с несбалансированными классами.
Пусть один класс составляет 99% данных, а другой – всего 1%. Тогда при использовании методов k-fold или holdout может возникнуть ситуация, когда в тестовую выборку не попадет ни один пример из редкого класса. Это делает невозможным оценку обобщающей способности модели на примерах редкого класса, что является существенным недостатком. Чтобы избежать этой проблемы, рекомендуется использовать стратификацию. 

**В задачах классификации:** стратификация означает сохранение пропорций классов в каждом фолде. 

Фолд – это каждое отдельное разбиение данных на обучающую и тестовую выборки.

На практике проводится случайный отбор из каждого класса в заданном соотношении. На рисунке ниже представлен пример стратификации для задачи бинарной классификации. Для класса "+" 70% данных отбираются в обучающую выборку, а 30% – в тестовую. Аналогично поступают с классом "-". Таким образом, пропорции классов в обучающей и тестовой выборках сохраняются такими же, как и в исходных данных.

@img02(cv_stratified2.jpg, Стратификация, 50)

**В задачах регрессии:** данные разбиваются на фолды так, чтобы среднее значение отклика было примерно одинаково в каждом фолде.

В регрессии редко возникает ситуация, когда все обучающие примеры сосредоточены в одной области, а тестовые – в другой. Если разбиение на обучающую и тестовую выборки выполняется случайным образом, то стратификация обычно не требуется. Если данные содержат небольшую группу примеров, значительно удаленную от основной массы, и эта группа не попадает в тестовую выборку, то, скорее всего, такие примеры являются выбросами. 

@NB(`~~Важно.~~ В задачах классификации редкий класс (например, 1% данных) не считается выбросом, а представляет собой просто малую долю данных.`)

В библиотечных функциях для реализации стратификации, как правило, достаточно указать параметр stratify=True. 

----
Оценка истинной ошибки модели
----

В результате кросс-валидации мы получаем выборку ошибок: $$R^*_{Tst}(h_1), ..., R^*_{Tst}(h_k),$$где $R^*_{Tst}(h_i)$ — это ошибка модели $h_i$ на тестовой выборке.

Следующим шагом является анализ этих ошибок.

@NB(`~~Важно.~~ В результате многократного разбиения данных при кросс-валидации мы оцениваем качество алгоритма обучения $μ$, а не конкретной обученной модели $h$.`)

**Истинная ошибка $R_{Tst}$** -- это ошибка модели на выборке новых данных бесконечного объема. Она представляет собой теоретическую меру качества модели, которую мы стремимся оценить.

Для оценки качества алгоритма используются следующие метрики:

1. **Точечная оценка**: <br>Среднее значение ошибок вычисляется как: $$\bar{R}{_{Tst}} = \frac{1}{k} \sum_{i=1}^k R^*_{Tst}(h_i).$$Эта оценка позволяет определить, насколько эффективен алгоритм в среднем. Если среднее значение ошибок неудовлетворительно, это указывает на необходимость пересмотра pipeline: возможно, требуется изменить данные, алгоритм обучения или другие параметры.<br>Оценка $\bar{R}{_{Tst}}$, полученная в результате кросс-валидации, имеет меньшее смещение относительно истинной ошибки $R_{Tst}$.

2. **Дисперсия**: <br>Дисперсия ошибок вычисляется как: $$s^2[R_{Tst}] = \frac{1}{k} \sum_{i=1}^{k} (R^*_{Tst}(h_i) - \bar{R}{_{Tst}})^2.$$Дисперсия показывает, насколько устойчивы результаты алгоритма к изменениям в обучающих данных. Высокая дисперсия указывает на то, что алгоритм сильно чувствителен к конкретным обучающим данным. Дисперсия может быть уменьшена путем увеличения объема обучающей и тестовой выборок.

Если среднее значение ошибок $\bar{R}{_{Tst}}$ находится на приемлемом уровне, а дисперсия $s^2[R_{Tst}]$ низкая, это свидетельствует о том, что все модели, обученные на различных фолдах, демонстрируют схожие обобщающие способности. В таком случае можно сделать вывод, что алгоритм не чувствителен к конкретным обучающим данным и способен выявлять объективные закономерности в данных.

~~Какую из $k$ моделей следует внедрить? ~~

Поскольку дисперсия ошибок низкая, все модели примерно одинаково хороши, и можно выбрать любую из них. Более того, в этом случае можно обучить модель на всем доступном датасете, не выделяя отдельную тестовую выборку. Это возможно, так как низкая дисперсия ошибок свидетельствует о том, что модель будет демонстрировать хорошие обобщающие способности независимо от конкретной обучающей выборки. Однако для дополнительной уверенности можно оставить небольшую тестовую выборку для финальной проверки.

----
Внутренняя кросс-валидация
----

Внешняя кросс-валидация предполагает многократное разбиение данных на обучающую и тестовую выборки. Однако в процессе обучения может быть выполнена процедура валидации модели, целью которой является выбор модели с наилучшими обобщающими способностями из некоторого множества.
Вместо одной валидационной выборки может быть
использована ~~кросс-валидация при обучении~~ (для выбора гиперпараметров или одной модели из нескольких).

Внутренняя кросс-валидация проводится в пределах одного фолда, то есть внутри обучающей выборки, выделенной для внешнего фолда. Например, внутри обучающей выборки внешнего фолда может быть выполнена дополнительная кросс-валидация, такая как k-fold. Это позволяет более точно оценить обобщающие способности модели и выбрать наилучшую конфигурацию перед тестированием на внешней тестовой выборке.

**Выбор типа кросс-валидации** (внешней и внутренней) зависит от задачи и доступных ресурсов. 

- Как правило, если внешняя кросс-валидация выполняется методом k-fold, то внутренняя может быть более простой, например, holdout. 
- И наоборот, если внешняя кросс-валидация выполняется методом holdout, то внутренняя может быть k-fold. 

@img02(cv_internal.jpg, Внутренняя кросс-валидация, 50)

📋 **Пример.** Рассмотрим пример, представленный на рисунке выше. 

- Внутренняя кросс-валидация: 5-fold.  
- Внешняя кросс-валидация: holdout.  

В рамках внешнего фолда (обучающей выборки) выполняется внутренняя кросс-валидация. На обучающей выборке обучается несколько моделей, каждая из которых проходит процедуру 5-Fold кросс-валидации. Для каждой модели процесс обучения включает следующие шаги:  

1. Модель обучается на 4 фрагментах данных.  
2. Пятый фрагмент используется для тестирования (валидации).  
3. Этот процесс повторяется для всех 5 фолдов, и для каждой модели вычисляется средняя ошибка на валидационных фолдах.  

После завершения внутренней кросс-валидации выбирается модель с наименьшей ошибкой на валидационных данных. Эта модель становится результатом процедуры обучения, так как валидация является частью процесса обучения. На выходе внешнего фолда получается наилучшая модель, которая затем тестируется на тестовых данных.  

В данном примере внешняя кросс-валидация выполняется методом holdout, что означает однократное разбиение данных на обучающую и тестовую выборки. Если бы использовался другой метод внешней кросс-валидации (например, k-fold), аналогичный процесс повторялся бы для каждого внешнего фолда. Однако в случае holdout процедура выполняется только один раз.

### ❓ Вопросы для самопроверки

1. Что такое обучение с учителем? В чём его отличие от обучения без учителя?
2. Какие два основных типа задач решаются в обучении с учителем? Приведите примеры для каждого типа.
3. Что такое входные данные ($𝒳$) и выходные данные ($𝒴$) в контексте обучения с учителем?
4. Какие типы признаков существуют? Приведите примеры для каждого типа.
5. Что такое гипотеза ($h$) в контексте обучения с учителем?
6. Почему мы ищем не саму функцию $F$, а её оценку $h$?
7. Что такое алгоритм обучения ($μ$)? Как он связан с гипотезой $h$?
8. Что такое индуктивный порог (inductive bias)? Почему он важен для обучения модели? Какие проблемы могут возникнуть, если индуктивный порог слишком слабый или слишком сильный?
9. Приведите примеры индуктивного порога для задачи регрессии и задачи классификации.
10. Что такое переобучение (overfitting) и недообучение (underfitting)? Какие признаки указывают на переобучение модели, а какие -- на недообучение? Какие методы можно использовать для борьбы с переобучением и недообучением?
11. Что такое функция потерь (loss function)? Как она связана с качеством модели?
12. Что такое эмпирический риск модели? Как он вычисляется? В чем состоит принцип минимизации эмпирического риска (ERM)?
13. Что такое смещение (bias) и дисперсия (variance) модели? Как они связаны с ошибкой модели? Как они влияют на переобучение и недообучение?
14. Что такое кросс-валидация? Какие методы кросс-валидации вы знаете?
15. Что такое стратификация в кросс-валидации? Почему она важна в задачах классификации?
16. Как оценить качество модели на основе результатов кросс-валидации?
17. Что такое истинный риск модели? Почему его нельзя вычислить напрямую?
18. Что такое внутренняя и внешняя кросс-валидация? В чём их различие?
19. В чем состоит принцип GIGO? Как он связан с качеством данных?

### ✅ Тест по разделу
-----

@quizQuestion(Соотнесите свойства слабого и сильного индуктивного порога:)

<!-- data-type="none"-->
| Слишком слабый индуктивный порог | Слишком сильный индуктивный порог |
| -------- | ------- |
| [[(Слабые) | Сильные]] предположения о классе моделей $ℋ$ | [[Слабые | (Сильные)]] предположения о классе моделей $ℋ$ |
| [[ Отсутствие гибкости| (Широкий класс моделей) ]] | [[(Отсутствие гибкости) | Широкий класс моделей]]|
| [[(Сильная чувствительность к данным) | Игнорирование данных]] | [[Сильная чувствительность к данным | (Игнорирование данных)]] |
| [[ Мало | (Много)]] степеней свободы | [[(Мало) | Много]] степеней свободы |

@quizQuestion(Выберите примеры признаков для классификации фруктов:)

<!-- data-randomize -->
[[x]] масса
[[x]] размер
[[x]] цвет
[[ ]] яблоко
[[ ]] апельсин

@quizQuestion(`Что можно сказать об обобщающей способности модели, исходя из следующих соотношений ошибки на обучающей выборке $Error_{train}$ и ошибки на валидационной выборке $Error_{val}$:`)

<!-- data-randomize -->
- [(Оптимальная модель) (Переобучение) (Недообучение)]
- [ (x) ( ) ( )]  $Error_{train}$ ≈ $Error_{val}$ 
- [ ( ) (x) ( )]  $Error_{train}$ ≪ $Error_{val}$
- [ ( ) ( ) (x)]  $Error_{train}$ высокая

@quizQuestion(`Какая статистическая характеристика позволяет оценить, насколько среднее значение предсказаний в точке $x$ отклоняется от истинного значения регрессионной функции в этой точке?`)

<!-- data-randomize -->
[(x)] Смещение (bias) модели
[( )] Дисперсия (variance) модели
[( )] Среднеквадратическая ошибка (MSE)
[( )] Шум ($\sigma^2_x$)

@quizQuestion(`Выберите методы кросс-валидации, подходящие под следующие случаи:`)

<!-- data-randomize -->
- [[Holdout]  [K-Fold] [Монте-Карло]]
- [ [x] [ ] [ ]]  Предварительные исследования 
- [ [ ] [x] [x]]  Окончательная оценка модели

@quizQuestion(`Какие из следующих утверждений верны для индуктивного порога?`)

<!-- data-randomize -->
[[x]] Индуктивный порог — это набор предположений, которые делают задачу машинного обучения корректной.
[[x]] Слабый индуктивный порог приводит к высокой дисперсии модели.
[[ ]] Сильный индуктивный порог всегда лучше слабого.
[[x]] Индуктивный порог определяет сложность модели.

@quizQuestion(`Какие из следующих утверждений описывают переобучение, а какие — недообучение?`)

<!-- data-randomize -->
[(Переобучение) (Недообучение)]
[(x) ( )] Модель показывает низкую ошибку на обучающей выборке, но высокую ошибку на тестовой выборке.
[( ) (x)] Модель показывает высокую ошибку как на обучающей, так и на тестовой выборке.
[( ) (x)] Модель слишком простая и не может уловить закономерности в данных.
[(x) ( )] Модель слишком сложная и "запоминает" шум в данных.

@quizQuestion(`Какие из следующих утверждений о смещении (bias) и дисперсии (variance) верны?`)

<!-- data-randomize -->
[[x]] Высокое смещение приводит к недообучению.
[[x]] Высокая дисперсия приводит к переобучению.
[[ ]] Увеличение сложности модели всегда уменьшает смещение.
[[x]] Увеличение объёма данных может уменьшить дисперсию.

@quizQuestion(`Сопоставьте методы кросс-валидации с их описаниями:`)

<!-- data-randomize -->
[(Holdout) (k-Fold) (Leave-one-out) (Монте-Карло)]
[(x) ( ) ( ) ( )] Данные разбиваются на обучающую и тестовую выборки один раз.
[( ) (x) ( ) ( )] Данные разбиваются на $k$ частей, каждая часть используется для тестирования один раз.
[( ) ( ) (x) ( )] Каждый пример используется для тестирования ровно один раз, а остальные — для обучения.
[( ) ( ) ( ) (x)] Данные случайным образом разбиваются на обучающую и тестовую выборки несколько раз

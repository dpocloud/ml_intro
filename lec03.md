<!--

author:     Alexander Trofimov
attribute:  **Editor:** Daria Gudkova
email:    
date:       21/02/2025
version:    0.1
language:   en
narrator:   English

icon:    ./icon.png
logo:    ./course_logo.png

comment:  Курс "Введение в машинное обучение"

link:     ./custom.css

import:   ./macro.md

-->


## Обучение с учителем: регрессия
-----

!?[Lecture03 Video](https://www.youtube.com/watch?v=8pTEmbeENF4)

Раздел даёт системное представление о подходах к регрессионному анализу, подчёркивая важность корректного выбора модели и методов оценки в зависимости от решаемой задачи и характера данных.

Формализуется постановка задачи регрессионного анализа с квадратичной функцией потерь, обсуждаются свойства функции регрессии и условия оптимальности регрессионных моделей. Рассматриваются основные задачи, виды и этапы регрессионного анализа. 

Детально разбираются простейшая и множественная линейные регрессии. Обсуждаются ключевые предположения регрессионного анализа, свойства МНК- оценок и теорема Гаусса-Маркова.

Анализируется проблема гетероскедастичности: методы диагностики и подходы к устранению. Обсуждаются альтернативные методы оценки при нарушении предположений регрессионного анализа, такие как взвешенный МНК и робастные методы.

Рассматриваются частные случаи нелинейных регрессионных моделей (GLM, нейросетевые регрессионные модели), а также способы их обучения.

---
<h3 style="text-align: center;">Содержание раздела</h3>

- Статистические модели и регрессия

  - Статистические модели

    - Понятие статистической модели
    - Теоретические и статистические модели
    - Примеры

  - Регрессионные модели

    - Постановка задачи обучения регрессионной модели 
    - Риск модели с квадратичной функцией потерь 
    - Функция регрессии
    - Оптимальность регрессионных моделей

  - Регрессионный анализ

    - Задачи, виды и этапы регрессионного анализа

- Метод наименьших квадратов

  - Простейшая линейная регрессия и OLS

    - Матрица плана и система нормальных уравнений
    - МНК-оценки параметров простейшей линейной регрессии
    - Ошибки и остатки модели
    - Предположения регрессионного анализа 
    - Свойства МНК-оценок: линейность, состоятельность, несмещенность, эффективность, нормальность
    - Теорема Гаусса-Маркова
    - Метод наименьших квадратов

  - Множественная линейная регрессия

    - Проекционная матрица
    - МНК-оценки параметров множественной линейной регрессии
    - Статистические характеристики МНК-оценок

- Другие методы обучения

  - Проблема гетероскедастичности

    - Статистические тесты на гетероскедастичность
    - Визуальный анализ гетероскедастичности остатков регрессионной модели
    - Подходы к устранению гетероскедастичности

  - Взвешенный метод наименьших квадратов (Weighted Least Squares, WLS)
  - Робастный регрессионный анализ

    - Метод наименьших модулей, LAR-регрессия
    - Постановка задачи и обучение
    - Использование взвешенного метода наименьших квадратов для обучения робастных моделей
    - Метод итеративного взвешивания (Iteratively Reweighted Least-Squares, IRLS)

  - Нелинейная регрессия

    - Нелинейная регрессионная модель
    - Нелинейный метод наименьших квадратов
    - Преобразование к линейности
    - Обобщенные линейные модели (GLM)
    - Нейросетевая регрессия

----

### Статистические модели

----
Теоретические и статистические модели
----

Как было сказано [ранее](#15), выделяются два основных подхода к построению моделей: model-based и data-driven.

Model-based подход предполагает построение модели на основе понимания сущности рассматриваемого объекта или процесса. Результатом такого подхода являются ~~теоретические модели~~, которые формируются с использованием известных законов, например, законов физики, математических формул или уравнений.

Data-driven подход основан на использовании данных для построения модели. В результате применения этого подхода получаются ~~статистические модели~~. Эти модели обучаются на основе данных, полученных в результате наблюдений за моделируемым объектом. Важно отметить, что от статистических моделей не требуется, чтобы они точно отражали внутренние закономерности объекта. Достаточно, чтобы их поведение внешне соответствовало поведению моделируемого объекта.

**Типы математических моделей:**

- Теоретические модели (физические, экономические и т.д.)
- Статистические модели (data-driven)

@img03(theoryVsStatisticalModels.png, Теоретические и статистические модели, 75)

**Статистические модели** – это математические модели, используемые для описания закономерностей, свойственных случайным величинам (СВ) или присутствующих в результатах их наблюдения. Такие модели позволяют описать, как объект преобразует входные данные в выходные, но не объясняют, почему это происходит. Статистическая модель представляет собой множество распределений вероятностей $𝒫=\{P\}$ случайной величины или вектора $X$, поскольку данные, на основе которых она строится, являются результатом наблюдений за случайными величинами.

Построение статистической модели заключается в описании распределения наблюдаемых случайных величин на основе имеющейся выборки данных. Однако, поскольку на основе выборки можно получить только оценку распределения или его параметров, это распределение обозначается как $\hat P$. Оцененное распределение $\hat P$ принадлежит некоторому классу распределений, который необходимо задать перед построением модели.

Статистическая модель называется ~~параметрической~~, если распределение $𝒫$ параметризовано: $$𝒫=\{P_𝜃,𝜃∈Θ\},$$
где $Θ$ – ~~пространство параметров~~.

В этом случае задача построения модели сводится к нахождению оценок параметров этого распределения. Нахождение оценок параметров -- более простая задача, чем поиск конкретного распределения среди всех возможных функций плотности или функций распределения. Однако для построения статистической модели необходимо задать класс распределений или способ их параметризации. Это требует дополнительных ~~статистических предположений~~ о виде модели. Например, можно задать жесткие ограничения, предположив, что распределение принадлежит классу нормальных распределений с нулевым математическим ожиданием.

Рассмотрим несколько примеров, как статистические модели позволяют описывать случайные наблюдения и делать выводы на основе данных.

----
Пример 1: биномиальная статистическая модель
----

**Эксперимент:**

На заводе изготавливаются детали, которые проверяются на наличие дефектов. 

$N$ – количество наблюдаемых изделий, $n$ – количество дефектных изделий среди них.

Какая статистическая модель может быть предложена для этого эксперимента?

~~Замечание.~~ Представим, что было отобрано $N=1000$ изделий, среди которых $n=10$ оказались дефектными. На первый взгляд, можно сделать вывод, что $1\%$ изделий являются дефектными, и, если это соответствует требованиям, процесс производства можно считать удовлетворительным. Однако такой подход не учитывает случайный характер данных. Если повторить эксперимент, количество дефектных изделий может оказаться другим: например, 0 или 25. Следовательно, делать выводы на основе одного случайного результата некорректно. Для анализа таких данных необходима статистическая модель.

**Описание:**

$X$ -- количество дефектных изделий среди $N$ отобранных,

$n$ -- наблюдение случайное величины $X$,

$p$ -- вероятность дефекта в отдельном изделии (неизвестна, её необходимо оценить). 

Вероятности для СВ $X$:
$$P(k,p) = P[X=k] = C ^{k} _{N} p^k(1-p)^{N-k}, \quad k∈\{0,1,...,N\}$$

СВ $X$ имеет биномиальное распределение $B(N,p)$, так как эксперимент соответствует схеме Бернулли: каждое изделие может быть либо дефектным, либо нет, с вероятностью 
$p$.

**Статистическая модель:**
$$X∼B(N,p)$$
$$𝒫= {P(k,p), \quad p∈[0,1]}$$

**Оценивание по данным:**

Конкретное распределение $P$ из $𝒫$ получается в результате ~~подбора (fitting)~~ параметра $p$ по имеющимся данным.
Оценкой вероятности дефекта является относительная частота дефектных изделий в выборке: $$\hat p = \frac{n}{N},$$ где $p$ – наблюдаемая доля дефектных изделий – ~~оценка (estimate)~~ вероятности $p$ дефекта отдельного изделия. Эта оценка является несмещённой, состоятельной и эффективной. 

**Вероятности для СВ $X$ после оценивания:**
$$\hat P(k) = C ^{k} _{N} \hat p^k(1- \hat p)^{N-k}, \quad k∈\{0,1,...,N\}$$

После оценки параметра $p$ модель становится полностью определённой, и мы можем использовать её для расчёта различных вероятностей, например, вероятности того, что количество дефектов окажется равным $10$, меньше $10$ или будет находиться в любом другом диапазоне.

----
Пример 2: мультиномиальная статистическая модель
----

**Эксперимент:**

Три игрока играют в карты $N=10$ раз. 1-й игрок выиграл в 2-х играх, 2-й игрок – в 3-х играх, а 3-й игрок – в 5-и играх. 

Какая статистическая модель может быть предложена для этого эксперимента?

**Описание:**

Для анализа таких данных используется мультиномиальная модель, которая обобщает биномиальное распределение на случай нескольких исходов. 

$X = (X_1,...,X_M)^T$ – количества игр, выигранных игроками ${1,...,M}$ соответственно, 

$p_j$ – вероятность победы $j$-го игрока (неизвестна), $j=\overline{1,M}$,

$M$ – число игроков.

Вероятности для случайного вектора $X$:

$$P(x)=P[X=x]=\frac{N!}{\prod_{j=1}^Mx_j!}\prod_{j=1}^Mp_j^{x_j},$$
где $x=(x_1,...,x_M)^T$, $x_j\in\{0,...,M\}$, $j=\overline{1,M}$, $\sum_{j=1}^Mx_j=N$.

**Статистическая модель:**

$$X\sim Mult(N,p_1,...,p_M),$$

$$\mathcal{P}=\left\{P_X(x,p),\  p\in[0,1]^M,\  \textstyle \sum_{j=1}^Mp_j=1\right\},$$

где $p=(p_1,...,p_M)$ -- вектор параметров.

**Оценивание по данным:** 

Оценка $\hat p_j$ вероятности $p_j$ -- частота побед $j$-го игрока:

$$\hat p_j=\frac{n_j}{N}, \quad j=\overline{1,M}$$

Для мультиномиального распределения оценками вероятностей успехов каждого игрока будут частоты их побед. В данном случае случайный вектор $X = (X_1, X_2, X_3)$ описывает количество побед каждого игрока. Мультиномиальная модель параметризуется вероятностями $p_1, p_2, p_3$, которые оцениваются по частотам побед: $\hat{p}_1 = \frac{2}{10}, \hat{p}_2 = \frac{3}{10}, \hat{p}_3 = \frac{5}{10}$.

**Вероятности для $X$ после оценивания:**

$$\hat P(x)=\frac{N!}{\prod_{j=1}^Mx_j!}\prod_{j=1}^M\hat p_j^{x_j}$$

С помощью этой модели можно делать какие-то выводы, которые мы не видим в данных изначально. Можно рассчитать вероятности различных исходов, например, вероятность того, что третий игрок не выиграет ни одной игры.

----
Пример 3: одновыборочная нормальная статистическая модель
----

**Экперимент:**

Предположим, что в научном эксперименте проводится $n$ измерений физической константы, например, ёмкости конденсатора. Результаты измерений являются случайными из-за погрешностей измерений. 

$x_1,...,x_n$ -- результаты независимых измерений физической константы $\mu$ в научном эксперименте.

Какая статистическая модель может быть предложена для этого эксперимента?

**Описание:**

Пусть СВ $X$ -- результат измерения физической константы $\mu$. Предположим, что ошибки измерения представляют собой аддитивный гауссов белый шум с дисперсией $\sigma^2$:
$$X=\mu+\varepsilon,$$
где $\varepsilon$ -- случайная величина, $\varepsilon\sim N(0,\sigma^2)$.

**Статистическая модель:**
$$X\sim N(\mu,\sigma^2),$$
$$\mathcal{P}=\left\{P(x)=\frac{1}{\sigma\sqrt{2\pi}}\exp\left(-\frac{(x-\mu)^2}{2\sigma^2}\right),\  \mu\in\mathbb{R},\  \sigma>0\right\},$$
где $\mu$ и $\sigma$ -- ~~неизвестные~~ константы. 

**Оценивание по данным:**

Оценками параметров $\mu$ и $\sigma^2$ являются выборочное среднее и выборочная дисперсия соответственно: 
$$\hat\mu=\bar{x}=\frac{1}{n}\sum\limits_{i=1}^nx_i,$$
$$\hat\sigma^2=s^2=\frac{1}{n-1}\sum\limits_{i=1}^n(x_i-\bar{x})^2.$$
После оценивания параметров модель становится полностью определённой, и её можно использовать для расчета вероятностей попадания наблюдаемой случайной величины в различные диапазоны, для построения доверительных интервалов и проверки статистических гипотез.

**Распределение СВ $X$ после оценивания:**
$$\hat P(x)=\frac{1}{s\sqrt{2\pi}}\exp\left(-\frac{(x-\bar{x})^2}{2s^2}\right).$$

----
Пример 4: двухвыборочная нормальная статистическая модель
----

**Эксперимент:**

Выборки $x_1,...,x_{n_1}$ и $y_1,...,y_{n_2}$ получены в результате независимых измерений одной и той же характеристики устройства до и после его настройки (например, расхода бензина автомобиля до и после ремонта).

Какая статистическая модель может быть предложена для этого эксперимента?

**Описание:**

Пусть СВ $X$ и $Y$ являются результатами измерений до и после настройки соответственно. Предположим, что характеристика устройства до настройки была равна $\mu_1$, а после настройки стала равна $\mu_2$. А также, что ошибки измерения представляют собой аддитивный гауссов белый шум с дисперсией $\sigma^2$:
$$X=\mu_1+\varepsilon, \quad Y=\mu_2+\varepsilon,$$
где $\varepsilon$ -- случайная величина, $\varepsilon\sim N(0,\sigma^2)$.

**Статистическая модель:**
$$X\sim N(\mu_1,\sigma^2), \quad Y\sim N(\mu_2,\sigma^2),$$
где $\mu_1$, $\mu_2$ и $\sigma$ -- ~~неизвестные~~ константы.

**Оценивание по данным:**

Модель для этого эксперимента предполагает, что измерения до и после настройки описываются нормальными распределениями с математическими ожиданиями $\mu_1$ и $\mu_2$ и одинаковой дисперсией $\sigma^2$:

$$\hat\mu_1=\bar{x}=\frac{1}{n_1}\sum\limits_{i=1}^{n_1}x_i, \quad \hat\mu_2=\bar{y}=\frac{1}{n_2}\sum\limits_{i=1}^{n_2}y_i,$$
$$\hat\sigma^2=s^2=\frac{(n_1-1)s_1^2+(n_2-1)s_2^2}{n_1+n_2-2},$$
где $s_1^2$ и $s_1^2$ -- оценки для $\sigma_1^2$ и $\sigma_2^2$ соответственно. Оценка $s^2$ называется ~~объединенной (pooled)~~ оценкой дисперсии $\sigma^2$.

**Распределения СВ $X$ и $Y$ после оценивания:**
$$X\sim N(\bar{x},s^2), \quad Y\sim N(\bar{y},s^2)$$

----
### Регрессионные модели
----

**Регрессионная модель** является частным случаем статистической модели. 

@img03(SchemaModel.png, Теоретические и статистические модели, 75)

$x \in 𝒳$ – вектор независимых переменных, называемых **регрессорами** (regressors or predictors, or explanatory variables).

$y \in 𝒴$ – зависимая переменная, называемая **откликом** (outcome or response). Множество откликов $𝒴=\mathbb{R}$.

$𝐿(h, (x, y))$ – **функция потерь** (loss function), связанная с моделью $h$.

----
Постановка задачи регрессии
----

**Дано:**

$𝒟_T=\{(x_1,y_1),...,(x_n,y_n)\}$ -- обучающая выборка,

$ℋ$ -- класс гипотез (например, класс линейных функций), 

$L(h,(x,y))=(h(x)-y)^2$ -- квадратичная функция потерь.

**Цель:**

Найти гипотезу $h\in ℋ$, минимизирующую эмпирический риск $R^*$ на обучающей выборке $𝒟_T$: 
$$R^\ast(h)=\frac{1}{n}\sum_{i=1}^n\left(h(x_i)-y_i\right)^2\rightarrow\min\limits_{h\inℋ}$$

Если $ℋ$ параметризовано, т.е. $ℋ=\{h_\beta,\beta\in\mathbb{R}^k\}$, тогда
$$R^\ast(\beta)=\frac{1}{n}\sum_{i=1}^n\left(h(x_i,\beta)-y_i\right)^2\rightarrow\min\limits_{\beta\in\mathbb{R}^k}$$

Отображение $F$, реализуемое моделируемым объектом, считается ~~стохастическим~~, а выход модели $h(x)$ детерминирован для любого $x \in 𝒳$.

**Риск с квадратичной функцией потерь** $L(h,(x,Y))$ при $x\in 𝒳$: 

$R(h,x) = M[L(h, (x, Y))|x] = M[(h(x)-Y)^2|x]=$<br>

$=h^2(x)-2h(x)M[Y|x]+M[Y^2|x]=$<br>

$=h^2(x)-2h(x)M[Y|x]+D[Y|x]+(M[Y|x])^2=$<br>

$=(h(x)-M[Y|x])^2+\sigma^2_x$,

где:

$(h(x)-M[Y|x])^2$ – **ошибка модели** $h$ для $x ∈ 𝒳$,

$\sigma^2_x=D[Y|x]$ – **шум**, не зависит от $𝒟$ и $h$.

**Риск $R(h,x)$ минимален, если**

$h(x)=M[Y|x],\forall x\in 𝒳 \Leftrightarrow h(x)$ -- ~~функция регрессии $Y$ на $x$~~.

В предыдущем [разделе](#15) мы показали, что цель обучения с учителем — смоделировать функцию регрессии, а не просто воспроизводить обучающие данные.

@quizQuestion(`Почему выход моделируемого объекта считается стохастическим?`)

<!-- data-randomize -->
[[ ]] Потому что это удобно для математических выкладок.
[[x]] Потому что на наблюдаемый выход объекта действуют случайные помехи, измерения могут проводиться на фоне шумов.
[[x]] Потому что во входном векторе могут быть учтены не все факторы, влияющие на выход моделируемого объекта.
[[ ]] Потому что модель объекта строится на основе данных.

----
Функция регрессии
----

Пусть $(X, Y)$ – случайный вектор, $(X, Y ) ∼ F_{XY} (x, y)$.

**Функцией регрессии $φ(x)$ СВ $Y$ на $x$** называется зависимость условного математического ожидания СВ $Y$ от $x$:
$$φ(x) = M[Y|x],\ x \in 𝒳$$

**Из теории вероятностей:**
$$M[Y|x] = \int_{-\infty}^{\infty}{yf_Y(y|x)dy}$$
$$f_Y(y|x)=\frac{f_{XY}(x,y)}{f_X(x)}$$
$$f_X(x)=\int_{-\infty}^{\infty}{f_{XY}(x,y)dy}, \ \ f_{XY}(x,y)=\frac{\partial F_{XY}(x,y)}{\partial x \partial y}$$

@img03(RegrFunc1.png, Функция регрессии Иллюстрация 1, 75)

Рассмотрим некоторую зависимость $y$ от $x$. Например, $x$ может представлять возраст, а $y$ — уровень дохода. При каждом фиксированном значении $x=x_i$ распределение $y$ принимает различный вид. Так как функция регрессии $y$ на $x$ отражает зависимость условного математического ожидания $y$ от $x$, то для её построения необходимо определить распределение $y$ при каждом $x$, вычислить соответствующее математическое ожидание и соединить полученные точки линией. Эта линия и будет функцией регрессии.

@img03(RegrFunc2.png, Функция регрессии Иллюстрация 2, 75)

Важно отметить, что распределения $y$ при разных $x$ могут существенно различаться, однако ключевым параметром остаётся именно условное математическое ожидание. Даже если распределения при всех $x$ идентичны (например, нормальные с одинаковой дисперсией), функция регрессии может быть нелинейной, так как математические ожидания способны изменяться произвольным образом в зависимости от $x$.

@img03(RegrFunc3.png, Функция регрессии Иллюстрация 3, 75)

На практике, особенно при работе с непрерывными переменными, редко встречаются ситуации, когда одному $x$ соответствует множество наблюдений $y$, что позволило бы построить условное распределение $p(y | x)$ и вычислить математическое ожидание $m(y|x)$. Однако, даже при наличии случайного рассеяния точек $(x,y)$, можно оценить функцию регрессии путем ~~локального усреднения~~ значений $y$ в небольших интервалах изменения значений переменной $x$: в окрестности заданной точки $x_0$ (например, $x_0 = 25 \pm \delta$) рассматривается подвыборка данных, по которой вычисляется выборочное среднее $y$. Повторяя эту процедуру для различных значений $x$ с достаточно малым $\delta$, можно восстановить оценку функции регрессии на всей области определения.

Аналогичный подход применим и для дискретной переменной $y$: при каждом фиксированном $x$ можно построить эмпирическое распределение $y$ (например, в виде гистограммы) и вычислить условное среднее значение $y$, что и определит регрессионную зависимость. На графике это может быть представлено в виде нелинейной функции регрессии, проходящей через усреднённые значения $y$ для каждого $x$.  

----
Оптимальность регрессионных моделей
----

**Оптимальность регрессионных моделей** объясняется тем, что воспроизведение функции регрессии минимизирует теоретический риск модели. Это следует из разложения случайной величины на две компоненты: **неслучайную функцию регрессии** и **случайный шум** с нулевым математическим ожиданием. 

**Оптимальный случай:**
$$h(x) = φ(x) = M[Y |x]$$

**Регрессионная модель отклика $Y$ при $x \in X$** :
$$Y |x = φ(x) + \varepsilon(x) = M[Y |x] + \varepsilon(x),$$
где $\varepsilon(x)$ – случайная величина (шум)

**Ошибка регрессии:**

$\varepsilon(x) = Y |x - M[Y |x]$

$M[\varepsilon(x)] = M[Y |x] - M[Y |x] = 0$  

$D[\varepsilon(x)] = M[\varepsilon(x)^2] = M[(Y |x - M[Y |x])^2] = D[Y |x] = \sigma_x^2$

Регрессионная модель имеет ~~минимальную дисперсию ошибок $D[\varepsilon(x)]$~~ среди всех статистических моделей.

~~Доказательство.~~
Если вместо истинной функции регрессии использовать произвольную зависимость, математическое ожидание квадрата отклонения $y$ от этой зависимости превысит $\sigma^2(x)$. Это подтверждается свойством минимальности среднеквадратичного отклонения: рассеяние относительно математического ожидания всегда меньше, чем относительно любой другой точки. Следовательно, дисперсия шума будет минимальной только в случае, когда модель точно воспроизводит функцию регрессии, а остаточная вариация сводится к случайному шуму.

----
### Регрессионный анализ
----

Регрессионный анализ изучает влияние одной или нескольких переменных $X_1, ..., X_k$ на результативную переменную $Y$.

**Цели регрессионного анализа** можно разделить на три ключевые категории.

1. **Описание или объяснение**.<br>Нахождение значимых регрессоров, оценка степени их влияния на отклик, описание закономерностей.<br>Например, если модель демонстрирует низкую точность, это может указывать на неадекватность выбранного класса моделей. Напротив, успешно обученная модель помогает определить, какие признаки (регрессоры) вносят наибольший вклад в предсказание отклика $y$, а какие статистически незначимы. Это позволяет формулировать выводы о структуре данных, которые изначально не были очевидны.

2. **Предсказание**.<br>Восстановление пропусков в данных в пределах диапазона изменения регрессоров или прогнозирование за пределами диапазона (экстраполяция).<br>Например, в случае временных рядов модель может прогнозировать будущие значения на основе исторических данных, что критически важно для анализа трендов.

3. **Вспомогательные цели**.<br>Сжатие данных, устранение линейных эффектов и пр.<br>Например, после построения модели можно извлечь остатки (шум), очищенные от линейного эффекта, и использовать их в других методах, таких как анализ аномалий или нелинейное моделирование.

----
Нахождение функции регрессии
----

Ключевой вопрос регрессионного анализа заключается в оценке неизвестной функции регрессии. Если совместное распределение $F_{XY} (x,y)$ известно, то функцию регрессии $φ(x)$ можно вывести аналитически. Однако на практике распределение $F_{XY} (x,y)$ обычно неизвестно, что требует оценки функции регрессии $φ(x)$ по данным.

Оцененная регрессионная модель:

$$Y | x = \hat{φ} ( x ) + \hat \varepsilon ( x ),$$
где $\hat φ(x)$ – оценка функции регрессии $φ(x)$ по имеющейся обучающей выборке.

Эта задача является ~~некорректно поставленной~~, так как точное восстановление функции регрессии требует знания плотности распределения $(X,Y)$, что невозможно на основе конечной выборки. 

Необходимы предположения о распределении $F_Y(y|x)$. Корректность этих предположений определяет качество оценки: верные допущения приводят к близости $\hat φ(x)$ к истинной функции, ошибочные — к значительным погрешностям.

----
Виды регрессионного анализа
----

**Виды регрессионного анализа** классифицируются по характеру допущений:

- **Параметрический (parametric)**<br>Семейство распределений $\{F_Y (y|x)\}$ полностью определено с точностью до параметров $\beta$, что приводит к параметрическому классу гипотез $H = \{h(x, \beta), \beta \in \mathbb{R}^k\}$. Например:
$$Y|x∼N(\beta_0 +\beta_1x,\sigma^2)$$

Задача сводится к оценке вектора параметров $\beta$. Дополнительно могут предполагаться свойства распределения $Y$ (нормальность), что влияет на выбор метода оценки (например, МНК).

- **Полупараметрический (semi-parametric)**<br>Семейство распределений ${F_Y (y|x)}$ частично параметризовано.Например, функция регрессии может быть параметризована, но распределение $Y$ остаётся неизвестным, что усложняет выбор критерия оптимизации:
$$M[Y|x]=\beta_0 +\beta_1x, D[Y|x]=\sigma^2$$

- **Непараметрический (non-parametric)**<br>Предположения о распределении $F_Y (y|x)$ отсутствуют. Оценка ищется непосредственно по данным, в широком классе функций (например, ядерная регрессия). Этот подход связан с методами непараметрического машинного обучения и применяется как в регрессии, так и в классификации.

----
Этапы регрессионного анализа
----
Регрессионный анализ следует стандартному процессу машинного обучения, адаптированному к его специфике. 

Основные этапы включают:

- **Шаг 1.** Определение отклика $Y$ и регрессоров $x_1, ..., x_k$.
- **Шаг 2.** Формулировка предположений о распределении $F_Y (y|x)$ и определение класса регрессионных моделей $H$, т.е. задание индуктивного порога.
- **Шаг 3.** Оценивание функции регрессии $φ(x)$ или ее параметров $\beta$ по выборке.
- **Шаг 4.** Диагностика (валидация) обученной регрессионной модели, оценка ее обобщающих способностей.
- **Шаг 5.** Интерпретация результатов.
- **Шаг 6.** Если необходимо, коррекция модели или предположений.
- **Шаг 7.** Использование модели.

----

### Простейшая линейная регрессия и OLS

----
Простейшая линейная регрессия
----

В простейшей линейной регрессии (simple linear regression) зависимость отклика $Y$ от регрессора $x$ моделируется как
$$Y|x=\beta_0 +\beta_1x+\varepsilon(x),$$
где $\beta_0,\beta_1$– параметры, оцениваемые по выборке, 
$\beta_0$ – свободный член (intercept), 
$\beta_1$ – коэффициент наклона (slope), 
$\varepsilon(x)$ – случайная ошибка с нулевым математическим ожиданием.

Функция регрессии $φ(x)$ предполагается линейной: 
$$φ(x)=M[Y|x]=\beta_0 +\beta_1x$$

Рассогласования между фактическими и модельными откликами называются **ошибками регрессии**:
$$\varepsilon_i =y_i -φ(x_i)=y_i -\beta_0 -\beta_1x_i, \quad i=\overline{1,n}$$

----
Метод наименьших квадратов (OLS)
----

**Метод наименьших квадратов (Ordinary least squares, OLS)** находит оптимальные значения параметров путем минимизации суммы (или среднего значения) квадратов ошибок:
$$E(\beta_0, \beta_1) = \frac{1}{n}\sum_{i=1}^n{(y_i-φ(x_i))^2} = \frac{1}{n}\sum_{i=1}^n{(y_i -\beta_0 -\beta_1x_i)^2}$$
Приравнивание градиента нулю:
$$\frac{\partial E(\beta_0, \beta_1)}{\partial \beta_0} = -\frac{1}{n}\sum{(y_i -\beta_0 -\beta_1x_i)}$$
$$\frac{\partial E(\beta_0, \beta_1)}{\partial \beta_1} = -\frac{1}{n}\sum{x_i(y_i -\beta_0 -\beta_1x_i)}$$
Система нормальных уравнений:
$$
\begin{cases}
\beta_0n+\beta_1\sum{x_i}=\sum{y_i}
\\
\beta_0\sum{x_i}+\beta_1\sum{x_i^2}=\sum{x_iy_i}
\end{cases}
$$
Система нормальных уравнений в матричном виде:
$$X^TX\beta = X^Ty$$
где
$\beta=(\beta_0,\beta_1)^T$ –вектор параметров,
$y=(y_1,...,y_n)^T$ –вектор откликов, 
$X$ – регрессионная матрица (design matrix):
$$
X=
\begin{pmatrix}
1 & x_1
\\
... & ...
\\
1 & x_n
\end{pmatrix}
$$
**Решение:**
$$\hat{\beta}=(X^TX)^{-1}X^Ty$$
**В скалярном виде:**
$$\hat{\beta_1}=\frac{\sum{(x_i-\overline{x})(y_i-\overline{y})}}{\sum{(x_i-\overline{x})}^2}=\rho_{XY}\frac{s_Y}{s_X},$$
где $\rho_{XY}$ – выборочный коэфф. корреляции, $s_X$ и $s_Y$ – с.к.о.

|Оптимальная функция регрессии| Неоптимальная функция регрессии|
|---|---|
| @img03(regr_1.png, Оптимальная функция регрессии, 100) | @img03(regr_2.png, Неоптимальная функция регрессии, 100) |
| Сумма квадратов отклонений наблюдаемых данных от модельных минимальна. | Большие отклонения наблюдаемых данных от модельных. |

OLS – метод оценивания параметров, на входе которого
данные, а на выходе – оценки.

@img03(ols1.png, OLS, 80)

Оценки $\hat \beta_0,\hat \beta_1$ являются случайными величинами, как и любые другие статистики, рассчитанные по выборке.

Следовательно, оценка $\hat φ(x) = \hat \beta_0 + \hat \beta_1x$ условного математического ожидания $φ(x) = M[Y |x]$ также будет случайной величиной.

📋 **Пример.** Покажем случайность оценки функции регрессии $\hat φ(x)$ на примере. Красная линия – истинная функция регрессии, синяя линия – оценка функции регрессии, обученная на подвыборке данных (выделенных черным цветом).

@img03(regr_4.png, OLS, 50)

**Регрессионные ошибки (regression errors)** $\varepsilon_1, ..., \varepsilon_n$ – отклонения наблюдений $y_1, ..., y_n$ отклика $Y$ от значений $φ(x_1), ..., φ(x_n)$ функции регрессии $φ(x)$:
$$\varepsilon_i=y_i-φ(x_i),\ i=\overline{1,n}$$
Т.к. функция регрессии $φ(x)$ неизвестна, то ошибки $\varepsilon_1, ..., \varepsilon_n$ также неизвестны.

**Случайные регрессионные ошибки** $\varepsilon_1, ..., \varepsilon_n$ – отклонения случайных величин $Y|x_1, ..., Y|x_n$ от значений $φ(x_1), ..., φ(x_n)$ функции регрессии $φ(x)$:
$$\varepsilon_i=Y|x_i-φ(x_i)=Y_i-φ(x_i),\ i=\overline{1,n}$$
Т.к. $Y|x_1, ..., Y|x_n$ – случайные величины, то ошибки $\varepsilon_1, ..., \varepsilon_n$ также являются случайными величинами.

**Регрессионные остатки (regression residuals)** $\hat \varepsilon_1, ..., \hat \varepsilon_n$ – различия между наблюдениями $y_1, ..., y_n$ и оцененными значениями $\hat φ(x_1), ..., \hat φ(x_n)$ функции регрессии $φ(x)$:
$$\hat \varepsilon_i=y_i-\hat y_i=y_i-\hatφ(x_i),\ i=\overline{1,n}$$
Регрессионные остатки наблюдаемы, они формируют выборку значений.

**Случайные регрессионные остатки** $\hat \varepsilon_1, ..., \hat \varepsilon_n$ – различия между случайными величинами $Y|x_1, ..., Y|x_n$ и оценками  
$\hat φ(x_1), ..., \hatφ(x_n)$ функции регрессии $φ(x)$:
$$\hat \varepsilon_i=Y|x_i-\hatφ(x_i)=Y_i-\hatφ(x_i),\ i=\overline{1,n}$$
Оценки $\hat φ(x_1), ..., \hatφ(x_n)$ также являются случайными величинами, т.к. 
$\hat φ(x_i) = \hat{\beta_0} + \hat{\beta_1}x_i$ и $\hat{\beta_0}, \hat{\beta_1}$
– случайные величины.

@quizQuestion(`Какие математические преимущества дает использование квадратов отклонений по сравнению с модулями или другими степенями?`)

<!-- data-randomize -->
- [(x)] Квадратичная функция дифференцируема
- [(x)] Квадратичная функция выпукла
- [(x)] Позволяет найти аналитическое решение через систему линейных уравнений
- [( )] Обеспечивает более точные результаты при любом распределении ошибок

----
Предположения регрессионного анализа
----

OLS – процедура расчета оценок коэффициентов $\beta$, не требующая никаких предположений о случайных ошибках  $\varepsilon_1, ..., \varepsilon_n$. Статистические предположения необходимы для того, чтобы делать статистические выводы о построенной регрессионной модели.

- **Линейность**<br>
Предполагается, что истинная функция регрессии $φ(x)$ линейна. Если это условие выполнено, оценки OLS будут обладать желаемыми свойствами. Однако если истинная зависимость нелинейна, оценки $\beta_0$ и $\beta_1$ теряют смысл, переставая соответствовать реальным параметрам модели.<br>
$$φ(x) = M[Y |x] = \beta_0 + \beta_1x$$
$$Y |x = φ(x) + \varepsilon(x) $$
$$M[Y |x] = M[φ(x) + \varepsilon(x)] = φ(x) + M[\varepsilon(x)]$$
Это эквивалентно центрированности случайных ошибок: если регрессия линейна, их математическое ожидание равно нулю.
$$M[\varepsilon(x)]=0 \ \ \ ∀x∈𝒳$$

- **Экзогенность регрессоров**<br>
Регрессоры $X$ и ошибки $\varepsilon(x)$ независимы $∀x \in X$ . Это означает, что ~~наблюдаемые значения регрессоров не зависят от ошибок модели~~.

Зависимость может возникнуть, например, в системах с обратной связью, где значения $x$ влияют на ошибки, а те, в свою очередь, определяют последующие $x$. В таком случае оценки OLS ухудшаются. Однако если данные представляют собой статичную выборку, полученную без использования обратных связей, это условие обычно выполняется.

- **Гомоскедастичность** (постоянство дисперсии)<br>
Дисперсия случайных ошибок должна быть постоянной при всех значениях $x$.
$$D[\varepsilon(x)] = \sigma^2\ \ \ ∀x \in X $$
Это означает, что отклик $Y$ также является ~~гомоскедастичным~~:
$$D[Y|x]=D[\beta_0 +\beta_1x+\varepsilon(x)]=D[\varepsilon(x)]=\sigma^2\ \ \ ∀x \in X$$
Если дисперсия ошибок изменяется (т.е. наблюдается гетероскедастичность), оценки OLS перестают быть оптимальными.<br>

- **Независимость ошибок**<br>
Любые ошибки $\varepsilon(x_i)$ и $\varepsilon(x_j)$ должны быть независимыми СВ. Это эквивалентно тому, что любая пара откликов $Y |x_i$ и $Y |x_j$ являются независимыми СВ. Более слабое требование – ~~некоррелированность~~:$$𝑐𝑜𝑣[\varepsilon(x_i),\varepsilon(x_j)]=𝑐𝑜𝑣[Y|x_i,Y|x_j]=0\ \ \ ∀x_i,x_j \in X, \ i\neq j$$
Например, если $y$ — это доход, а $x$ — возраст, то доходы людей разного возраста не должны коррелировать между собой. Нарушение этого условия (например, при наличии автокорреляции) приводит к неэффективности OLS оценок.

- **Нормальность**<br>
Требуется, чтобы случайные ошибки $ε$ при каждом фиксированном значении $x$ имели нормальное распределение.
$$\varepsilon(x)~N(0,\sigma^2)\ \ \ ∀x \in X$$
Это условие эквивалентно нормальности распределения зависимой переменной $y$ при каждом $x$:
$$Y|x\sim N(\beta_0+\beta_1x,\sigma^2)\ \ \ ∀x \in X $$
Например, если $y$ — это доход, а $x$ — возраст, то распределение доходов для каждой возрастной группы должно быть близко к нормальному.

Нарушение этого предположения (например, при наличии тяжелых хвостов или асимметрии) может ухудшить свойства оценок OLS, особенно для малых выборок. Однако при больших объемах данных (благодаря центральной предельной теореме) оценки остаются состоятельными, хотя их эффективность может снижаться.

~~Замечание.~~ В случае нормальности независимость и некоррелированность ошибок эквивалентны.

- **Вариабельность регрессоров**<br>
Регрессоры $X$ должны обладать ненулевой дисперсией. Регрессия не имеет смысла, если $X$ является константой.

@NB(`~~Важно.~~ Если хотя бы одно из этих требований нарушено, оценки OLS могут стать смещенными, неэффективными или несостоятельными. В таких случаях необходимо применять альтернативные методы обучения параметров регрессионных моделей.`)

📋 **Пример.** Предположения о линейности, постоянстве дисперсии и нормальности выполнены.
@img03(regr_3.png, Предположения выполнены, 60)

📋 **Пример.**  Нарушения предположений.

**1. Нарушение линейности**

@img03(ex1.png, Нарушение линейности, 40)
Истинная зависимость имеет нелинейный (квадратичный) характер. Если в этом случае применить обычный OLS, полученные оценки $\beta_0$ и $\beta_1$ не будут соответствовать реальным параметрам модели. Решением может быть переход к полиномиальной или нелинейной регрессии.

**2. Гетероскедастичность (нарушение постоянства дисперсий)**

@img03(ex2.png, Нарушение гомоскедастичности, 40)

Дисперсия ошибок изменяется в зависимости от $x$. 
Непостоянная ширина облака данных относительно линии регрессии.

В таких условиях OLS-оценки теряют эффективность. Рекомендуется использовать:

- взвешенный метод наименьших квадратов (WLS);
- робастные стандартные ошибки;
- преобразования данных (например, логарифмирование).

**3. Нарушение нормальности распределения ошибок**

@img03(ex3.png, Нарушение нормальности, 40)

При некоторых $x$ прослеживается бимодальное распределение.

Использование OLS в таком случае приводит к:

- снижению точности оценок;
- проблемам при построении доверительных интервалов.

Альтернативные подходы:

- обобщенные линейные модели (GLM);
- непараметрические методы;
- удаление выбросов (если они искажают распределение).

**4. Нарушение требования о независимости ошибок**

@img03(ex4.png, Нарушения независимости ошибок, 40)

Визуально по этому графику наличие корреляции между наблюдениями определить сложно. Для диагностики можно использовать график "остатки $\hat \varepsilon_1, ..., \hat \varepsilon_{n-1}$ vs. остатки $\hat \varepsilon_2, ..., \hat \varepsilon_n$". В условиях зависимости ошибок модели OLS-оценки теряют эффективность.

----
Свойства OLS-оценок
----

Рассмотрим некоторые ~~свойства OLS-оценок~~, которые получаются при выполнении тех или иных предположений.

**1. Линейность**

~~Предположение:~~ линейность.

При выполнении предположения о линейности истинной функции регрессии, OLS-оценки обладают свойством линейности по откликам.

Коэффициенты $\beta_0, \beta_1$ – ~~линейные оценки~~ (т.е. линейные функции наблюдаемых откликов $y_1, ..., y_n$): 
$$\hat\beta = ( X^T X )^{-1} X^T y = B y,$$
где $B=(X^TX)^{-1}X^T$ – матрица размера $2×n$,
$$\hat \beta_0 =(1\ 0)\hat \beta = (1\ 0)By=\sum_{i=1}^n {b_{0i}y_i}$$
$$\hat\beta_1 = (0\ 1)\hat\beta = (0\ 1)By = \sum_{i=1}^n{b_{1i}y_i} = \sum_{i=1}^n\frac{x_i-\bar{x}}{\sum_{i=1}^n(x_i-\bar{x})^2}y_i$$

❓ Попробуйте самостоятельно вывести выражение для $b_{0i}$.

**2. Состоятельность**

~~Предположения:~~ линейность, экзогенность регрессоров.

Состоятельность означает, что с увеличением объема выборки оценки сходятся по вероятности к истинным значениям оцениваемых параметров. Это свойство обеспечивается некоррелированностью регрессоров и ошибок.

Оценки $\hat \beta_0, \hat \beta_1$ являются ~~состоятельными~~:
$$\hat\beta_0\xrightarrow{P}\beta_0,\ \ \hat\beta_1\xrightarrow{P}\beta_1$$
$$P[|\hat\beta_j-\beta_j|<\delta]\rightarrow1\hspace{5mm}\forall\delta>0\hspace{5mm} as \hspace{5mm} n\rightarrow\infty, \hspace{5mm} j\in\{0,1\}$$

~~Доказательство:~~
$$\begin{aligned}
\hat\beta_1&=\frac{\sum(x_i-\bar x)Y_i}{\sum(x_i-\bar x)^2}=\frac{\sum(x_i-\bar x)(\beta_0+\beta_1x_i+\varepsilon_i)}{\sum(x_i-\bar x)^2} \\
&=\frac{\sum(x_i-\bar x)}{\sum(x_i-\bar x)^2}\beta_0+\frac{\sum(x_i-\bar x)x_i}{\sum(x_i-\bar x)^2}\beta_1+\frac{\sum(x_i-\bar x)\varepsilon_i}{\sum(x_i-\bar x)^2} \\
&=0+\beta_1+\frac{\sum(x_i-\bar x)\varepsilon_i}{\sum(x_i-\bar x)^2}\rightarrow \beta_1+\frac{{cov[X,\varepsilon]}}{D[X]}=\beta_1+0=\beta_1
\end{aligned}$$

❓ Попробуйте самостоятельно доказать состоятельность $\hat \beta_0$.

**3. Несмещенность**

~~Предположение:~~ линейность.

Оценки $\hat\beta_0$, $\hat\beta_1$ являются ~~несмещенными~~ (т.е. их математические ожидания равны истинным значениям оцениваемых параметров):
$$M[\hat\beta_0]=\beta_0, \hspace{5mm}M[\hat\beta_1]=\beta_1$$

~~Доказательство:~~<br>
$$M[\hat\beta_1]=M\left[\frac{\sum(x_i-\bar x)Y_i}{\sum(x_i-\bar x)^2}\right]=\frac{\sum(x_i-\bar x)M[Y_i]}{\sum(x_i-\bar x)^2}=$$
$$=\frac{\sum(x_i-\bar x)(\beta_0+\beta_1x_i+M[\varepsilon_i])}{\sum(x_i-\bar x)^2}$$
$$=\frac{\sum(x_i-\bar x)}{\sum(x_i-\bar x)^2}\beta_0+\frac{\sum(x_i-\bar x)x_i}{\sum(x_i-\bar x)^2}\beta_1=0+\beta_1=\beta_1$$

❓ Попробуйте самостоятельно доказать несмещенность $\hat \beta_0$.

**4. Эффективность**

**Предположения:** линейность, экзогенность регрессоров, гомоскедастичность, независимость ошибок.

Оценки дисперсий оценок:
$$D[\hat\beta_0]=\frac{\sigma^2\sum{x_i^2}}{n\sum(x_i-\bar{x})^2}=\frac{\sigma^2\alpha_{2X}^*}{nD_X^*}$$
$$D[\hat\beta_1]=\frac{\sigma^2}{\sum(x_i-\bar{x})^2}=\frac{\sigma^2}{nD_X^*}$$

❓ Попробуйте самостоятельно доказать это.


@Block(`Теорема Гаусса-Маркова`,`Из всех линейных несмещенных оценок OLS-оценки являются ~~наиболее эффективными~~, т.е. они имеют наименьшую дисперсию. Более того, в предположении о нормальности они являются наиболее эффективными ~~среди всех несмещенных оценок~~.`)

**5. Нормальность**

**Предположения:** линейность, гомоскедастичность, независимость ошибок, нормальность ошибок.

Оценки $\hat\beta_0$, $\hat\beta_1$ являются ~~нормально распределенными~~ СВ:
$$\hat\beta_0\sim N\left(\beta_0,\frac{\sigma^2\alpha^*_{2X}}{nD^*_X}\right),\hspace{5mm}\hat\beta_1\sim\left(\beta_0,\frac{\sigma^2\alpha^*_{2X}}{nD^*_X}\right)$$
Если ошибки $\varepsilon(x)$ не являются нормально распределенными, то распределения СВ $\hat\beta_0, \hat\beta_1$ при большом объеме выборки ~~близки к нормальным~~.

Дисперсия $\sigma^2$ случайных ошибок $\varepsilon(x)$ обычно неизвестна. Можно показать, что ~~остаточная дисперсия~~ является несмещенной оценкой $\sigma^2$:
$$S^2_e=\frac{1}{n-2}\sum_{i=1}^{n}(y_i-\hat y_i)^2=\frac{1}{n-2}\sum_{i=1}^{n}(y_i-\hat\beta_0-\hat\beta_1x_i)^2$$
и $\frac{(n-2)S^2_e}{\sigma^2}\sim\chi^2(n-2)$ и СВ $S^2_e$ и $\hat\beta_0,\hat\beta_1$ независимы.

~~Замечание.~~ Свойство нормальности можно продемонстрировать с помощью метода Монте-Карло, многократно генерируя выборки из известного распределения и вычисляя оценки.

----
Метод максимального правдоподобия (ММП)
----

**Метод максимального правдоподобия (ММП)** — это стандартный подход в математической статистике, используемый для оценки неизвестных параметров статистической модели (при которых получение данной выборки является наиболее вероятным событием). 

Например, в случае нормального распределения наблюдаемых данных с неизвестными параметрами (математическим ожиданием $μ$ и дисперсией $σ²$), ММП позволяет вывести формулы для их оценок.

Так, оценка математического ожидания случайной величины вычисляется как выборочное среднее. Хотя эта формула кажется интуитивно понятной, её строгое обоснование следует именно из ММП. Аналогично можно оценивать и более сложные параметры. ММП широко применяется в машинном обучении, поскольку он ~~статистически обоснован~~.

Основная идея ММП заключается в построении **функции правдоподобия**, которая зависит от выборки наблюдений $x_1, \ldots, x_n$ и неизвестных параметров статистической модели. По определению, функция правдоподобия представляет собой совместную плотность распределения вероятностей вектора наблюдений.  

Для независимых и одинаково распределённых наблюдений многомерная плотность представляет собой произведение одномерных плотностей. Например, если наблюдения следуют нормальному распределению, то функция правдоподобия будет иметь вид:  

$$\mathscr{L}(\theta \mid x_1, \ldots, x_n) = \prod_{i=1}^n f(x_i \mid \theta),$$
где $\theta$ — вектор параметров модели.  

ММП предполагает поиск таких значений параметров, при которых вероятность получения данной выборки максимальна. На практике удобнее максимизировать не саму функцию правдоподобия, а её логарифм, что упрощает вычисления за счёт замены произведения на сумму:  

$$\ln \mathscr{L}(\theta \mid x_1, \ldots, x_n) = \sum_{i=1}^n \ln f(x_i \mid \theta)\rightarrow\max_{\theta}$$

----
Связь ММП и МНК (OLS)
----

**Предположения:** линейность, гомоскедастичность, независимость ошибок, нормальность ошибок.

В этом случае оценки $\hat\beta_0$, $\hat\beta_1$ являются МП-оценками параметров $\beta_0$, $\beta_1$.

**Статистическая модель:**
$$\varepsilon(x)\sim N(0,\sigma^2), \hspace{5mm} \varepsilon(x)=Y|x-\beta_0-\beta_1x$$

**Выборка:**
$$\hspace{5mm}\varepsilon_1,...,\varepsilon_n,\hspace{5mm} \varepsilon_i =y_i -\beta_0 -\beta_1x_i,\hspace{5mm} i=\overline{1,n}$$

**Функция правдоподобия (Likelihood function):**
$$\mathscr{L}(\varepsilon_1,...,\varepsilon_n,\beta_0,\beta_1)=\prod_{i=1}^{n}f_{\varepsilon}(\varepsilon_i|\beta_0,\beta_1)$$

МП-оценки параметров $\beta_0, \beta_1$ – решение оптимизационной задачи:

$$\mathscr{L}(\varepsilon_1,...,\varepsilon_n,\beta_0,\beta_1)\rightarrow\max_{\beta_0,\beta_1}$$

**Логарифмическая функция правдоподобия:**

$$\ln\mathscr{L}(\varepsilon_1,...,\varepsilon_n,\beta_0,\beta_1)=\sum_{i=1}^n{\ln f_{\varepsilon}\left(\varepsilon_i|\beta_0,\beta_1\right)}\rightarrow \max\limits_{\beta_0,\beta_1}$$
$$\begin{aligned}
\ln\mathscr{L}&(\varepsilon_1,...,\varepsilon_n,\beta_0,\beta_1)=\sum_{i=1}^n\ln\left[\frac{1}{\sigma\sqrt{2\pi}}\exp\left(-\frac{\varepsilon_i^2}{2\sigma^2}\right)\right] \\
&=n\ln\left(\frac{1}{\sigma\sqrt{2\pi}}\right)-\frac{1}{2\sigma^2}\sum_{i=1}^n\varepsilon_i^2=c-\frac{1}{2\sigma^2}\sum_{i=1}^n(y_i-\beta_0-\beta_1x_i)^2
\end{aligned}$$
$$\begin{cases}
\frac{\partial\ln\mathscr{L}(\varepsilon_1,...,\varepsilon_n,\beta_0,\beta_1)}{\partial\beta_0}=\frac{1}{\sigma^2}\sum(y_i-\beta_0-\beta_1x_i)=0 \\
\frac{\partial\ln\mathscr{L}(\varepsilon_1,...,\varepsilon_n,\beta_0,\beta_1)}{\partial\beta_1}=\frac{1}{\sigma^2}\sum x_i(y_i-\beta_0-\beta_1x_i)=0 \\
\end{cases}$$

Это та же система линейных уравнений, что и в OLS!

@NB(`~~Важно.~~ При выполнении указанных предположений МНК не является произвольным выбором, а имеет строгое статистическое обоснование. Оценки коэффициентов $\beta_0$ и $\beta_1$, полученные методом максимального правдоподобия, совпадают с МНК-оценками.`)

❓ Попробуйте самостоятельно вывести выражение для МП-оценки $\sigma^2$.

@quizQuestion(`Какие предположения должны выполняться, чтобы МНК имел строгое статистическое обоснование?`)

<!-- data-randomize -->
- [(x)] линейность
- [(x)] гомоскедастичность
- [(x)] независимость ошибок
- [(x)] нормальность ошибок

----
Доверительные интервалы для параметров регрессии
----
Поскольку оценки параметров являются случайными величинами, для них можно рассчитать дисперсию. Оценка дисперсии ошибок модели (остаточная дисперсия) обозначается как $S_e^2$. 

**Дисперсии оценок:**
$$S[\hat\beta_0]=\frac{S_e^2\alpha_{2X}^*}{nD_X^*},\ \  S[\hat\beta_1]=\frac{S_e^2}{nD_X^*}$$

Зная дисперсию оценок коэффициентов регрессии, можно стандартизовать их, преобразовав в статистики, имеющие известные распределения. Если оценка коэффициента $\hat\beta_j$ является несмещённой, то разность $\hat\beta_j-\beta_j$​ (где $\beta_j$ — истинное значение) имеет нулевое математическое ожидание.

**Центральная статистика:**
$$T_j=\frac{\hat\beta_j-\beta_j}{S[\hat\beta_j]}\sim T(n-2), \hspace{5mm} j\in\{0,1\}$$

Таким образом, зная распределение оценок коэффициентов, можно построить доверительные интервалы для параметров модели. Все необходимые границы вычисляются на основе выборочных данных.

**Доверительные интервалы (ДИ) для** $\beta_j, j\in\{0,1\}$:
$$P[\hat\beta_j-t_{1-\frac{\alpha}{2},n-2}S[\hat\beta_j]<\beta_j<\hat\beta_j+t_{1-\frac{\alpha}{2},n-2}S[\hat\beta_j]]=1-\alpha,$$
где $(1 - \alpha)$ – уровень значимости, $t_{1-\frac{\alpha}{2},n-2}$ – $(1 - \alpha/2)$-я квантиль распределения Стьюдента с $n - 2$ степенями свободы

----
ДИ и интервалы предсказания для функции регрессии
----

**$(1 - \alpha)$-доверительный интервал для условного мат. ожидания $φ(x)$:**
$$\varphi(x)\in[\hat\beta_0+\hat\beta_1x\mp t_{1-\frac{\alpha}{2},n-2}S_e\sqrt{\frac{1}{n}+\frac{(x-\bar x)^2}{nD^*_X}}]$$

**$(1 - \alpha)$-интервал предсказания для $Y|x$:**
$$\varphi(x)\in[\hat\beta_0+\hat\beta_1x\mp t_{1-\frac{\alpha}{2},n-2}S_e\sqrt{1+\frac{1}{n}+\frac{(x-\bar x)^2}{nD^*_X}}]$$

**Доверительный интервал** определяет интервал значений для неизвестного условного мат. ожидания $M[Y |x]$.

**Интервал предсказания** определяет интервал, в котором будут находиться наблюдения СВ $Y |x$. 

Интервал предсказания всегда ~~шире~~, чем соответствующий доверительный интервал, поскольку он учитывает не только неопределённость в оценке математического ожидания, но и случайную вариацию $Y$ вокруг этого математического ожидания.

@img03(conf_2.png, ДИ и интервалы предсказания 1, 75)

ДИ – для условного мат. ожидания $\varphi(x) = M[Y |x]$, интервал предсказания – для наблюдений СВ $Y |x$.

@img03(conf_1.png, ДИ и интервалы предсказания 2, 75)

Чем дальше $x$ от $\bar x$, тем шире интервалы.

Если какое-либо из статистических предположений (линейность, гомоскедастичность, независимость ошибок, нормальность ошибок) нарушено, то ДИ и интервалы предсказания ~~могут оказаться неправильными~~. Проверка статистических предположений – одна из задач диагностики регрессионных моделей.

----
Статистические гипотезы о параметрах регрессии
----

Одна из стандартных задач статистического анализа регрессионных моделей — проверка гипотез о значимости ее коэффициентов.

**Статистические гипотезы:**
$$H_0:\beta_j=0\hspace{5mm}vs\hspace{5mm}H':\beta_j \neq 0,\hspace{5mm}j\in\{0,1\}$$

**Статистика критерия:**
$$Z_j=\frac{\hat\beta_j}{S[\hat\beta_j]},\hspace{5mm}Z_j|H\sim T(n-2),\hspace{5mm}j\in\{0,1\}$$

@img03(gr5.png, Статистические гипотезы о параметрах регрессии, 75)

Проверка гипотез необходима, потому что выборочные оценки коэффициентов почти никогда не равны нулю точно, даже если истинное значение $\beta_j$ нулевое. Статистические тесты позволяют сделать вывод о значимости параметров на основе данных, а не полагаться лишь на конкретные численные значения оценок.

@quizQuestion(Что означает принятие нулевой гипотезы $H_0: \beta_j = 0$ в регрессионном анализе?)

<!-- data-randomize -->
- [( )] Признак $x_j$ оказывает сильное влияние на зависимую переменную $y$, и его необходимо оставить в модели.
- [(x)] Признак $x_j$ статистически незначим, и его можно исключить из модели, без существенной потери качества.  
- [( )] Модель содержит гетероскедастичность, и требуется её коррекция.
- [( )] Доверительный интервал для коэффициента $\beta_j$ не включает ноль, что подтверждает его значимость.

----
###  Множественная линейная регрессия
----

Случай, когда моделируемая величина $y$ зависит только от одной переменной $x$, довольно редко встречается на практике. В этом разделе мы обобщим простейшую линейную регрессию на случай нескольких регрессоров. Заметим, что множественная линейная регрессия подразумевает линейность по $\beta$ (а не по $x$).

В **множественной линейной регрессии** отклик $Y$ зависит от множества регрессоров $X_1,...,X_k$:
$$Y|x=\beta_0 +\beta_1x_1 +...+\beta_kx_k +\varepsilon(x)=x\beta+\varepsilon(x),$$
где $x=(1,x_1,...,x_k)$ – вектор регрессоров, $\beta = (\beta_0,...,\beta_k)^T$ – вектор оцениваемых параметров.

Функция регрессии $\varphi(x)$ предполагается **линейной**:
$$φ(x) = M[Y |x] = x\beta $$

**Регрессионные ошибки**:
$$\varepsilon_i =y_i -φ(x_i)=y_i -x_i\beta, \ \ \ i=\overline{1,n},$$
где $x_i = (1, x_{1i}, ..., x_{ki})$ – вектор значений регрессоров в $i$-м наблюдении

----
OLS для множественной линейной регрессии
----

Метод наименьших квадратов применяется так же, как и в простейшей линейной регрессии: минимизируется сумма квадратов отклонений наблюдаемых значений $y$ от модельных, лежащих на функции регрессии (в данном случае — гиперплоскости).

@img03(regr_11.png, Множественная линейная регрессия Иллюстрация, 40)

**Критерий OLS:**
$$E(\beta) = \frac{1}{n}\sum_{i=1}^n{(y_i-φ(x_i))^2} = \frac{1}{n}\sum_{i=1}^n{(y_i - x_i\beta)^2}\rightarrow \min_{\beta}$$

Приравнивание градиента нулю:
$$\frac{\partial E(\beta_0,...,\beta_j)}{\partial\beta_j}=0,\hspace{5mm}j=\overline{0,k}$$

**Система нормальных уравнений:**
$$X^TX\beta=X^Ty,$$
где $y = (y_1, ..., y_n)^T$ – вектор откликов, $X$ – регрессионная матрица (design matrix):
$$
X=
\begin{pmatrix}
1 & x_{11} & ... & x_{k1}
\\
... & ... & ... & ...
\\
1 & x_{1n} & ... & x_{kn}
\end{pmatrix}
$$

Система уравнений, получаемая приравниванием частных производных к нулю, аналогична случаю с одним регрессором, но регрессионная матрица теперь имеет размерность $n × (k + 1)$, где дополнительный столбец соответствует свободному члену $\beta_0$.

**Замечание.** При использовании библиотечных функций для регрессии важно учитывать, как они обрабатывают свободный член. Некоторые функции требуют явного задания столбца из единиц, другие автоматически добавляют его при указании параметра intercept=True. Если столбец не задан, модель может обучаться без свободного члена ($\beta_0 = 0$), поэтому необходимо проверять документацию конкретной функции.

**Решение:**
$$\hat\beta=(X^TX)^{-1}X^Ty$$

**Модельные отклики:**
$$\hat{y}=X\hat\beta=X(X^TX)^{-1}X^Ty=Hy,$$
где матрица $H=X(X^TX)^{-1}X^T$ называется **проекционной матрицей (hat matrix)**.

Модельные отклики $\hat y$ – ~~линейные функции ~~наблюдаемых откликов $y$.

**Вектор остатков:**
$$\hat\varepsilon=y-\hat y=y-Hy=(I-H)y,$$
где $\hat\varepsilon=(\hat\varepsilon_1,...,\hat\varepsilon_n)^T,\hspace{5mm} \hat\varepsilon_i=\hat y_i-y_i,\hspace{5mm} i=\overline{1,n}$.

Ковариационная матрица оценок $\hat\beta$:
$$C_{\hat\beta}=cov[\hat\beta]=\sigma^2(X^TX)^{-1},$$
где $\sigma^2$ – ~~дисперсия случайных ошибок~~, $\sigma^2 = D[\varepsilon(x)]$.

Можно показать, что ~~дисперсия остатков (остаточная дисперсия)~~ является несмещенной оценкой $\sigma^2$:
$$s^2_e=\frac{1}{n-k-1}\sum_{i=1}^{n}(y_i-\hat y_i)^2=\frac{1}{n-k-1}\sum_{i=1}^{n}(y_i-x_i\hat\beta)^2$$
и $\frac{(n-k-1)S^2_e}{\sigma^2}\sim\chi^2(n-k-1)$, и $s^2_e$ не зависит от $\hat\beta_0,...,\hat\beta_k$.

**Оценка ковариационной матрицы $\hat C_{\hat\beta}$:**
$$\hat C_{\hat\beta}=s^2_e(X^TX)^{-1}$$

----
Доверительные интервалы для параметров регрессии
----

Доверительные интервалы (ДИ) в многомерном случае строятся аналогично простейшей линейной регрессии, их границы так же не являются линейными — они представляют собой поверхности, расходящиеся по мере удаления от центра данных.

@img03(regr_12.png, ДИ и ИП Множественная регрессия, 30)

**Дисперсии оценок:**
$$S^2[\hat\beta_j]=\hat c_{jj},\hspace{5mm}j=\overline{0,k},$$
где $\hat c_{jj}$ – $j$-й диагональный элемент матрицы $\hat C_{\hat\beta}$.

**Центральные статистики для** $\beta_j,\ j=\overline{0,k}$: 
$$T_j=\frac{\hat\beta_j-\beta_j}{S[\hat\beta_j]}\sim T(n-k-1),\hspace{5mm}k=\overline{0,k}$$

**Доверительные интервалы для** $\beta_j,\ j=\overline{0,k}$:
$$
P[\hat\beta_j-t_{1-\frac{\alpha}{2},n-k-1}S[\hat\beta_j]<\beta_j<\hat\beta_j+t_{1-\frac{\alpha}{2},n-k-1}S[\hat\beta_j]]=1-\alpha,
$$
где $(1 - \alpha)$ – уровень значимости, $t_{1-\frac{\alpha}{2},n-k-1}$ – $(1 - \alpha/2)$-я квантиль распределения $T(n - k - 1)$.

----
ДИ и интервалы предсказания для функции регрессии
----

**$(1 - \alpha)$ -- доверительные интервалы для условного мат. ожидания $\varphi(x)$:**
$$
\varphi(x)\in[x\hat\beta\mp t_{1-\frac{\alpha}{2},n-k-1}S_e\sqrt{x(X^TX)^{-1}x^T}]
$$

**$(1 - \alpha)$ -- интервал предсказания для $Y |x$:**
$$
Y|x\in[x\hat\beta\mp t_{1-\frac{\alpha}{2},n-k-1}S_e\sqrt{1+x(X^TX)^{-1}x^T}]
$$

**Доверительный интервал** определяет интервал значений для неизвестного условного мат. ожидания $M[Y |x]$.

**Интервал предсказания** определяет интервал, в котором будут находиться наблюдения СВ $Y |x$.

----
Использование признаков в регрессионной модели
----

Множественную регрессию можно построить даже на основе одного исходного признака, преобразовав его в несколько новых. Например, из переменной $x$ можно создать полиномиальные признаки $(x², x³, ...)$, которые затем использовать в качестве регрессоров. Несмотря на их зависимость, это допустимо, если они не являются линейно зависимыми (иначе матрица $XᵀX$ окажется вырожденной, и обратную матрицу вычислить не удастся).

Связь между откликом $Y$ и регрессорами $X_1, ..., X_k$ может моделироваться как
$$Y|x=\beta_0+\beta_1f_1(x)+...+\beta_mf_m(x)+\varepsilon(x),$$
где $f(x) = (1,f_1(x),...,f_m(x))$ – вектор **признаков (features)**.

**Система нормальных уравнений в OLS:**
$$X^TX\beta=X^Ty,$$
где $y=(y_1,...,y_n)^T$ -- вектор откликов, $\beta=(\beta_0,...,\beta_m)^T$ – вектор параметров, $X$ -- **регрессионная матрица (design matrix)**:
$$
X=
\begin{pmatrix}
1 & f_1(x_1) & ... & f_m(x_1)
\\
... & ... & ... & ...
\\
1 & f_1(x_n) & ... & f_m(x_n)
\end{pmatrix}
$$

Решение системы и статистический вывод ~~такие же, как и для множественной линейной регрессии~~.

📋 **Пример.** Полиномиальная регрессия
$$
\begin{aligned}
\varphi(x)=\beta_0+\beta_1x+\beta_2x^2
\\
f(x)=(1,x,x^2)
\\
X=
	\begin{pmatrix}
	1 & x_1 & x^2_1
	\\
	... & ...
	\\
	1 & x_n & x^2_n
	\end{pmatrix}
\end{aligned}
$$

📋 **Пример.** Линейная регрессия со смешанным членом
$$
\begin{aligned}
\varphi(x)=\beta_0+\beta_1x_1+\beta_2x_2+\beta_3x_1x_2
\\
f(x)=(1,x_1,x_2,x_1x_2)
\\
X=
	\begin{pmatrix}
	1 & x_{11} & x_{21} & x_{11}x_{21}
	\\
	... & ...
	\\
	1 & x_{1n} & x_{2n} & x_{1n}x_{2n}
	\end{pmatrix}
\end{aligned}
$$

На рисунке ниже представлены линейные (~~по параметрам~~) регрессионные модели разных степеней (от простейшей линейной регрессии до полиномиальной регрессии с полиномом 10-й степени).

@img03(poly1.png, Одномерные регрессионные поверхности с полиномиальными признаками, 70)

На следующем рисунке изображен двумерный случай регрессионной поверхности с полиномиальными признаками.

@img03(poly2.png, Двумерные регрессионные поверхности с полиномиальными признаками, 70)

----
Проверка статистических гипотез
----
**I. Гипотезы о параметрах регрессии:**	
$$H_0:\beta_j=0\hspace{5mm}vs\hspace{5mm}H':\beta_j\ne0,\hspace{5mm}j\in\{0,k\}$$	

Статистика критерия:
$$Z_j=\frac{\hat\beta_j}{S[\hat\beta_j]},\hspace{5mm}Z_j|H_0\sim T(n-2),\hspace{5mm}j\in\{0,k\}$$

**II. Гипотеза о значимости модели:**
$$H_0:\beta_1=...=\beta_k=0\hspace{5mm}vs\hspace{5mm}H':\beta_1^2+...+\beta_k^2>0$$	

Статистика критерия:
$$Z=\frac{T^2/k}{(1-R^2)/(n-k-1)},\hspace{5mm}Z|H_0\sim F(k,n-k-1),$$
где $R^2$ -- ~~коэффициент детерминации~~:	
$$R^2=1-\frac{\sum(y_i-\hat y_i)^2}{\sum(y_i-\bar y)^2}=1-\frac{(n-k-1)s^2_e}{nD^*_Y}$$

----
### Метод взвешенных наименьших квадратов
----

----
Важность предположений регрессионного анализа
----

**Линейная регрессионная модель:**
$$Y|x=x\beta+\varepsilon(x)$$

**Предположения линейного регрессионного анализа:**

- Линейность
- Экзогенность регрессоров 
- Гомоскедастичность 
- Независимость ошибок 
- Нормальность 
- Вариабельность регрессоров

Если эти предположения нарушены, статистические выводы ~~могут оказаться неверными!~~

Предположения должны быть проверены в результате **разведочного анализа данных** или после обучения регрессионной модели.

Разведочный анализ данных может в себя включать:

- Изучение распределения признаков (гистограммы, описательные статистики).
- Анализ корреляций между переменными.
- Выявление выбросов и аномалий.
- Построение диаграмм рассеяния и анализ визуальных паттернов (например, можно проверить линейность).

----
Проблема гетероскедастичности
----

**Гетероскедастичность** означает, что условная дисперсия отклика не является константой:
$$D[Y|x]\ne const \Leftrightarrow D[\varepsilon(x)]\ne const$$

**Почему гетероскедастичность нежелательна при построении регрессионной модели?**

- OLS-оценки становятся неэффективными, т.е. нет гарантий, что они являются лучшими возможными оценками среди несмещенных оценок.
- Расчет дисперсий оценок параметров модели становится неверным, следовательно, ДИ, интервалы прогнозирования и статистики критерия становятся неверными.

----
Методы обнаружения гетероскедастичности
----
Обнаружение гетероскедастичности – одна из задач ~~диагностики регрессионных моделей~~.

- **Визуальный анализ**

  - Диаграммы рассеяния (в основном для двумерных данных)

@img03(het_4.jpg, Гетероскедастичность Иллюстрация, 50)

- **Статистические тесты на гетероскедастичность**

  - White test
  - Breush-Pagan test 
  - Park test  
  - Glejser test 
  - Goldfeld-Quandt test
  - ...

----
Статистические тесты на гетероскедастичность
----

Статистические тесты обычно применяются к регрессионным ошибкам $\varepsilon_1, ..., \varepsilon_n$, и проверяют нулевую гипотезу о постоянстве дисперсии ошибок (или зависимой переменной $y$, поскольку они связаны линейным преобразованием):
$$H_0:D[\varepsilon_1]=...=D[\varepsilon_n]=\sigma^2$$

**Идея:** Если выборка $\varepsilon_1, ..., \varepsilon_n$ гетероскедастична, то выборка квадратов $\varepsilon_1^2, ..., \varepsilon_n^2$ (или $|\varepsilon_1|, ..., |\varepsilon_n|$) имеет значимую линейную регрессию на модельный отклик $\hat y$ и/или регрессоры $x_1, ..., x_k$.

Рассмотрим стандартную линейную регрессию $y$ на $x$. Даже при наличии гетероскедастичности мы можем построить OLS-оценки коэффициентов $\beta_0$ и $\beta_1$, хотя они и не будут оптимальными. После построения модели мы получаем остатки $\hat\varepsilon = y - ŷ$.

@img03(het_1.png, Классическая линейная регрессия, 40)

Анализируя эти остатки, можно выявить гетероскедастичность:

1. Рассчитываем абсолютные значения или квадраты остатков ($|\hat\varepsilon|$ или $\hat\varepsilon^2$).
2. Строим линейную регрессию этих величин ($|\hat\varepsilon|$ или $\hat\varepsilon^2$) от $x$. 
@img03(het_2.png, Гетероскедастичность остатков, 40)
3. Смотрим на вид графика и/или проверяем значимость коэффициента $\beta_1$ построенной регрессионной модели:

- При гомоскедастичности график должен представлять собой горизонтальную линию, $\beta_1$ незначим.
- При гетероскедастичности будет наблюдаться явный тренд:

  - возрастающий: если $\beta_1$ значимо отличается от 0 (положительный) → дисперсия растет с увеличением $x$;
  - убывающий: если $\beta_1$ значимо отличается от 0 (отрицательный) → дисперсия уменьшается с увеличением $x$.

Такой подход имеет **ограничение**: позволяет выявить только линейную зависимость дисперсии от $x$.
Для более сложных случаев можно использовать:

- Полиномиальную регрессию (например, $|\hat\varepsilon|$ на $x$ и $x^2$).
- Тест Уайта, который учитывает квадраты и взаимодействия переменных.

**Альтернативные подходы к визуализации:**

|График зависимости остатков от предсказанных значений $ŷ$|График зависимости остатков от номера наблюдения (временной ряд)|
|---|---|
|@img03(residPlot.png, График зависимости остатков от предсказанных значений, 60)|@img03(indexPlot.png, График зависимости остатков от номера наблюдения, 50)|

В идеальном случае (при выполнении предположения о гомоскедастичности) эти графики должны демонстрировать случайное распределение точек без явных паттернов или трендов.

----
Подходы к устранению гетероскедастичности
----

После обнаружения гетероскедастичности можно использовать три основных подхода для ее устранения.

- **Преобразование отклика $y$**<br> Например, логарифмическое преобразование. Если после преобразования данные становятся гомоскедастичными, то можно использовать стандартный метод наименьших квадратов (OLS) для построения регрессионной модели $log(y)$ на $x$.

- **Обучение модели методом взвешенных наименьших квадратов**<br>Использование дополнительных неотрицательных констант (весов), связанных с каждым наблюдением, в критерии обучения.

- **Heteroscedasticity-consistent standard error estimators**<br>Расчет ковариационной матрицы оценок параметров регрессии с поправкой на гетероскедастичность. В рамках данного курса эти методы мы рассматривать не будем, предлагаем ознакомиться с некоторыми из них самостоятельно, изучив статьи на эту тему [Hayes A.F., Cai L. Using heteroskedasticity-consistent standard error estimators in OLS regression: An introduction and software implementation // Behavior research methods. 2007, 39(4), 709-722.].

----
Метод взвешенных наименьших квадратов WLS
----

**Линейная регрессионная модель:**
$$Y|x=x\beta+\varepsilon(x)$$

В методе взвешенных наименьших квадратов ~~модифицируется критерий обучения~~: каждому наблюдению присваивается вес $w_i$, и задача сводится к нахождению оптимальных параметров $\beta$, минимизирующих взвешенную сумму квадратов ошибок. Если все веса равны единице, метод сводится к обычному МНК (OLS).  

Критерий **взвешенных наименьших квадратов (Weighted Least Squares, WLS)**:
$$E(\beta)=\frac{1}{n}\sum_{i=1}^{n}w_i(y_i-x_i\beta)^2\rightarrow\min_{\beta},$$
где $w_i$ -- **вес (weight, scaling factor)** $i$-го наблюдения, $i=\overline{1,n}$.

**Система нормальных уравнений в матричном виде:**
$$X^TWX\beta=X^TWy$$

**Решение:**
$$\hat\beta=(X^TWX)^{-1}X^TWy,$$
где $W = diag(w_1, ..., w_n)$ -- диагональная **матрица весов**.

Заметим, что если $W$ — единичная матрица, то решение совпадает с формулой OLS.

**Как выбрать веса $w_1, ..., w_n$?**

**Идея:** выбрать веса, при которых дисперсии отклика становятся постоянными:

$$Y_i=x_i\beta+\varepsilon_i \quad\Rightarrow\quad D[Y_i]=D[x_i\beta+\varepsilon_i]=D[\varepsilon_i]=\sigma_i^2, \quad i=\overline{1,n}$$
$$\frac{Y_i}{\sigma_i}=\frac{x_i}{\sigma_i}\beta+\frac{\varepsilon_i}{\sigma_i}$$
$$Y'_i=x'_i\beta+\varepsilon'_i,$$
где
$$Y'_i=\frac{Y_i}{\sigma_i}, \quad x'_i=\frac{x_i}{\sigma_i}, \quad \varepsilon'_i=\frac{\varepsilon_i}{\sigma_i}$$
Преобразованная модель является **гомоскедастичной**:
$$Y'_i=x'_i\beta+\varepsilon'_i\quad\Rightarrow\quad D[Y'_i]=D[\varepsilon'_i]=1, \quad i=\overline{1,n}$$

----
Веса в WLS
----

Применим OLS к преобразованной модели:
$$\hat\beta=(X'^TX')^{-1}X'^Ty',$$
где  
$$X'=\Sigma^{-1}X,\hspace{5mm}y'=\Sigma^{-1}y,\hspace{5mm}\Sigma=diag(\sigma_1,...,\sigma_n)$$
Таким образом,
$$\hat\beta=(X^T\Sigma^{-1}\Sigma^{-1}X)^{-1}X^T\Sigma^{-1}\Sigma^{-1}y$$
и матрица весов должна быть
$$W=\Sigma^{-1}\Sigma^{-1}=(\Sigma^{-1})^2,$$
т.е. индивидуальные веса отдельных наблюдений должны быть равны ~~обратным дисперсиям остатков~~:
$$w_i=\frac{1}{\sigma_i^2},\hspace{5mm}i=\overline{1,n}$$

----
Подходы к выбору весов
----

На практике дисперсии остатков $\sigma_1^2,...,\sigma_n^2$ обычно неизвестны. Возможны следующие подходы.

- Предположение некоторой **модели весов**, основанной на визуальном анализе и априорной информации, например, $w_i=\frac{1}{x_i},\ i=\overline{1,n}$.
- Если значения $X$ дискретны, то возможно **оценивание дисперсий по выборке** для каждого дискретного значения $x_i$.
- Если значения $X$ непрерывны, то оценивание дисперсий по выборке не рекомендуется из-за возможных выбросов и неточности оценивания.
- Построение OLS-регрессии, расчет остатков $\hat\varepsilon_1^2,...,\hat\varepsilon_n^2$ и построение **регрессии $\hat\varepsilon^2$ или $|\hat\varepsilon|$ на $x$ или $\hat y$**. Модельные отклики этой регрессии могут использоваться вместо оценок дисперсии остатков.

Хорошая практика – попробовать различные подходы и выбрать наилучший, основываясь на распределении остатков.

---
📋 **Пример.** 

@img03(weight1.png, WLS Иллюстрация 1, 40)

На представленной иллюстрации показаны данные, в которых каждому значению $x$ соответствует несколько наблюдений $y$. Это позволяет непосредственно оценить дисперсию остатков для каждого $x$.

На графике отображены три линии регрессии:

- Истинная зависимость (красная линия).
- Оценка OLS (зелёная линия) - демонстрирует заметное смещение.
- Взвешенная оценка WLS (синяя линия) - практически совпадает с истинной функцией.

~~Вывод.~~ Учет гетероскедастичности позволяет получить существенно более точные оценки параметров регрессии по сравнению со стандартным OLS. В данном случае WLS практически устраняет смещение, характерное для невзвешенного метода.

---
📋 **Пример.** 

@img03(weight2.png, WLS Иллюстрация 2, 40)

На графике видно, что:

1. **Интервалы OLS**:

   - Не отражают реальную изменчивость данных.
   - Дают статистически неадекватные результаты (например, заявленный 90%-й интервал прогнозирования может покрывать существенно меньшее количество точек).

2. **Интервалы WLS**:

   - Существенно шире, чем при использовании OLS.
   - Учитывают неоднородность дисперсии.
   - Являются статистически корректными (например, заявленный 90%-й интервал прогнозирования содержит около 90% наблюдений).

~~Вывод.~~ При использовании обычного метода наименьших квадратов (OLS) в условиях гетероскедастичности построенные доверительные интервалы и интервалы предсказания становятся некорректными.

----
Замечания WLS
----

1. В WLS предполагается, что **ковариационная матрица ошибок диагональная $cov[\varepsilon]=diag(\sigma_1^2,...,\sigma_n^2)$**.<br>Для любых двух различных наблюдений $x_i$ и $x_j$ дисперсии ошибок могут быть различными ($Var(ε_i) ≠ Var(ε_j)$). Однако ковариация между ошибками различных наблюдений предполагается равной нулю $Cov(ε_i, ε_j)=0$ при $i≠j$.<br>⇒ WLS применим ~~только~~ в случаях, когда нарушена исключительно гомоскедастичность, но сохранена некоррелированность ошибок.

2. Для применения WLS нужно **определить веса** $w_1,...,w_n$.<br>В большинстве статистических библиотек это реализовано через параметр функции `fit`, например `sampleWeights`, который принимает массив весов для каждого наблюдения. По умолчанию всем наблюдениям присваивается единичный вес, что эквивалентно обычному методу OLS.

3. Если веса выбраны равными **обратным дисперсиям остатков**, то WLS выравнивает дисперсии ошибок $\sigma_1^2,...,\sigma_n^2$, т.е. устраняет гетероскедастичность.

4. Наблюдения с **низкой дисперсией** будут давать **больший вес**, а наблюдения с **высокой дисперсией** – **меньший вес** в критерии обучения: более точные данные (с меньшим разбросом) оказывают большее влияние на итоговую оценку параметров.

5. Решение WLS **такое же**, что и у OLS, примененного к преобразованным данным.

6. WLS удобен для **исключения некоторых наблюдений** из обучения путем приравнивания их веса нулю.<br>Это дает возможность работы без явного разделения на train/test через `train_test_split`. Вся выборка передается в функцию `fit`. Обучающим наблюдениям присваивается вес $1$. Тестовым наблюдениям присваивается вес $0$.

5. Путем уменьшения веса выбросов или влиятельных точек, можно **уменьшить их влияние на модель**.

----
Обобщенный метод наименьших квадратов GLS
----

В **обобщенном методе наименьших квадратов (Generalized least squares, GLS)** предполагается, что ковариационная матрица ошибок $cov[\varepsilon]$ может быть ~~недиагональной~~.

**Линейная регрессионная модель:**
$$Y|x=x\beta+\varepsilon(x)$$

**Критерий GLS:**
$$E(\beta)=(y-X\beta)^TW(y-X\beta)\rightarrow\min_{\beta},$$
где $W$ – ~~недиагональная матрица весов~~. Если $W=cov[\varepsilon]$, то GLS эквивалентен OLS для преобразованной модели:
$$Y_i'=x_i'\beta+\varepsilon_i',$$
где
$$X'=\Sigma^{-1}X,\hspace{5mm}y'=\Sigma^{-1}y,\hspace{5mm}\varepsilon'=\Sigma^{-1}\varepsilon$$
и $\Sigma$ – ~~квадратный корень~~ ковариационной матрицы $cov[\varepsilon]=\Sigma\Sigma^T$.

В преобразованной системе координат ошибки становятся гомоскедастичными и некоррелированными.

Получение точных оценок всех элементов ковариационной матрицы по ограниченной выборке практически невозможно. На практике GLS чаще используют либо с упрощенными моделями ковариаций, либо когда структура зависимостей между ошибками известна априорно из особенностей моделируемых данных.

----
📋 **Пример.**

Введем следующие предположения о ковариационной матрице:  

1. Диагональные элементы матрицы (дисперсии) обратно пропорциональны весам, как в WLS.  
2. Ненулевые ковариации существуют только между соседними ошибками $\varepsilon_{i}$ и $\varepsilon_{i-1}$.  

В таком случае матрица весов $W$ в GLS примет следующий вид:  

- На главной диагонали будут значения $\frac{1}{\sigma_i^2}$.  
- Ненулевые элементы будут расположены непосредственно над и под главной диагональю (ковариации соседних ошибок).  
- Остальные элементы матрицы равны нулю.  

----
Отбеливающее преобразование
----

Преобразование, которое превращает коррелированные данные в некоррелированные, осуществляется с помощью линейного преобразования — умножения на матрицу.

Пусть случайный вектор $\varepsilon=(\varepsilon_1,...,\varepsilon_n)^T$ , $M[\varepsilon] = 0$, имеет ковариационную матрицу $cov[\varepsilon]=\Sigma\Sigma^T$ . Тогда преобразование
$$\varepsilon'=\Sigma^{-1}\varepsilon$$
называется **отбеливающим преобразованием (whitening transformation)**.

Преобразованные СВ $\varepsilon_1',...,\varepsilon_n'$ являются ~~некоррелированными СВ с единичной дисперсией~~.

📋 **Пример.**

1. Пусть дана двумерная нормально распределенная случайная величина $\varepsilon=(\varepsilon_1,\varepsilon_2)$, $\varepsilon_1$ и $\varepsilon_2$ коррелированы между собой. Их рассеяние направлено вдоль главной диагонали эллипса.

2. С помощью линейного преобразования можно перейти к новой системе координат $\varepsilon_1'$ и $\varepsilon_2'$, повернув оси так, чтобы главная ось эллипса рассеяния совпадала с одной из новых координатных осей. В результате $\varepsilon_1'$ и $\varepsilon_2'$ становятся некоррелированными.

3. Чтобы сделать их гомоскедастичными, каждую ось можно растянуть или сжать так, чтобы дисперсия стала единичной, также используя линейное преобразование. В этом случае эллипс рассеяния превращается в круг, и если исходное распределение было нормальным, то получается круговое нормальное распределение.

@img03(white1.png, Отбеливающее преобразование, 70)

В многомерном случае преобразование заключается в «выравнивании» эллипсоида вдоль осей координат. По аналогии с взвешенным МНК (WLS), переход к новой системе координат позволяет получить ошибки, которые являются некоррелированными и гомоскедастичными. Такое преобразования осуществляется с помощью матрицы $\Sigma^{-1}$.

Заметим, что для одномерного случая отбеливающее преобразование сводится к делению на $σ$, поскольку ковариационная матрица имеет вид $σ^2$, а обратный корень из нее — это $\frac{1}{σ}$. В многомерном случае необходимо умножить на обратный корень из ковариационной матрицы.

**GLS regression = error whitening + OLS regression**

----
Замечания GLS
----

1. WLS – частный случай GLS, в котором ковариационная матрица $cov[\varepsilon]$ предполагается диагональной.
2. Для применения GLS нужно знать все ковариации $cov[\varepsilon_i,\varepsilon_j],\ i,j=\overline{1,n}$.
3. На практике ковариационная матрица $cov[\varepsilon]$ обычно неизвестна. Не рекомендуется использовать оценку ковариационной матрицы $cov[\varepsilon]$, лучше предположить некоторую ~~модель для ковариаций~~ с небольшим числом оцениваемых параметров.
4. Решение GLS такое же, что и у OLS, примененного к преобразованным данным.
5. GLS-оценки являются несмещенными, состоятельными, эффективными и асимптотически нормальными, т.к. OLS применяется к модели с гомоскедастичными некоррелированными ошибками, т.е. условия теоремы Гаусса-Маркова выполнены.

----
### Робастные регрессионные модели
----

В OLS, WLS и GLS предполагается, что ~~ошибки модели имеют нормальное распределение~~. Однако если нормальность ошибок нарушена, статистические свойства оценок становятся неоптимальными.

Проверить, соответствует ли распределение нормальному, можно с помощью классических критериев согласия, таких как критерий Колмогорова-Смирнова, или статистических тестов, например теста Харке-Бера.

Существует множество вариантов отклонения данных от нормальности, что затрудняет выбор подходящего метода преобразования. Например, если данные имеют логнормальное распределение, то логарифмирование переводит их в нормальное распределение. 

Одним из частных случаев отклонения от нормальности является наличие **выбросов**, которые могут сделать распределение мультимодальным. Выбросы оказывают большое влияние на LS-модели, т.к. возведение остатков в квадрат увеличивает их эффект.

Один из способов исправить ситуацию -- удалить выбросы на этапе предобработки данных. Это может потребовать применения, например, методов обучения без учителя (unsupervised learning), которые автоматически определяют и удаляют выбросы.

Альтернативный подход – использование **робастных методов регрессии**, устойчивых к выбросам. Этот вариант может оказаться предпочтительнее, так как он позволяет избежать применения вспомогательных методов устранения выбросов и настройки их гиперпараметров. Среди методов робастной регрессии можно выделить:

- **Least absolute residuals (LAR)**
- **Iteratively reweighted least-squares (IRLS)**
- **RANSAC-регрессия**<br>Пытается отделить данные от выбросов и обучить модель только на инлаерах
- **Регрессия Тейла-Сена (Theil-Sen regression)**<br>Обучение множества регрессионных моделей на подвыборках и комбинирование результатов
- ...

В рамках данного курса основное внимание уделяется методам LAR и IRLS.

----
LAR-регрессия
----

**Идея:** использовать $L_1$-норму в критерии обучения.

**Линейная регрессионная модель:**
$$Y|x=x\beta+\varepsilon(x)$$

**Least Absolute Residuals (LAR) критерий**:
$$E(\beta)=\frac{1}{n}\sum_{i=1}^n|y_i-x_i\beta|\rightarrow\min_{\beta}$$

Использование модуля вместо квадратов делает метод более устойчивым к выбросам по сравнению с OLS. Если точка сильно отклоняется от основной массы данных, ее вклад в сумму квадратов ошибок в OLS становится значительным, что заставляет модель смещаться в сторону этой точки. Однако модульная функция недифференцируема в нуле, ее производная принимает значения $-1$ или $+1$, что затрудняет аналитическое нахождение минимума критерия.

В отличие от OLS, который имеет аналитическое решение, для обучения LAR-регрессии используются итерационные методы оптимизации (симплекс-метод, градиентный спуск и т.д.). 

Решение LAR-регрессии ~~не единственно~~, оно зависит от начальной точки и используемого алгоритма оптимизации.

Чтобы избежать недостатков LAR-регрессии, могут быть использованы альтернативные методы робастной регрессии. Один из подходов – взвешенный метод наименьших квадратов (WLS). 

----
WLS для робастной регрессии
----

**Идея:** использовать взвешенный квадратичный критерий, в котором вес наблюдения зависит от того, насколько оно далеко от функции регрессии.

- Точкам, расположенным в основной массе данных, присваивается вес 1.  
- Точкам, далеким от основной выборки, присваивается малый или нулевой вес. 

**Как измерить расстояние от наблюдения до функции регрессии?**

@img03(conf_3.png, WLS для робастной регрессии, 50)

Ключевая проблема – определение удаленности точки от основной массы данных.
Евклидово расстояние не является лучшей метрикой, поскольку ~~дисперсия остатков зависит~~ от значений регрессоров. Необходимо использовать относительные величины для оценки удаленности точки от функции регрессии с поправкой на дисперсию.

Возникает еще один вопрос: какой порог считать границей, отделяющей выбросы от нормальных точек? Определение относительных остатков и порога для их классификации – две основные задачи, которые необходимо решить для корректного применения WLS в условиях выбросов.

----
Дисперсии остатков
----

**Регрессионные остатки:**
$$\hat\varepsilon_i=y_i-\hat y_i=y_i-x_i\hat\beta,\hspace{5mm}i=\overline{1,n},$$
где $\hat\beta=(X^TX)^{-1}X^Ty$ – OLS-оценка $\beta$.

В предположениях о гомоскедастичности и некоррелированности ошибок:
$$D[\varepsilon]=D[Y|x_i]=\sigma^2,\hspace{5mm}cov[\varepsilon_i,\varepsilon_j]=0,\hspace{5mm}\forall i,j=\overline{1,n},\hspace{5mm}i\ne j$$

Дисперсии остатков $\hat\varepsilon=(\hat\varepsilon_1,...,\hat\varepsilon_n)^T$:
$$D[\hat\varepsilon]=D[Y-X(X^TX)^{-1}X^TY]=(I-H)D[Y]=(I-H)\sigma^2$$

**Замечание.** Матрица $I-H$ является идемпотентной ($(I - H)^2 = I - H$), поэтому её можно вынести за знак дисперсии без возведения в квадрат.

В скалярной форме это приводит к выражению:
$$D[\hat\varepsilon_i]=\sigma^2(1-h_i),\hspace{5mm}i=\overline{1,n}$$
где $H=X(X^TX)^{-1}X^T$ -- **проекционная матрица (hat matrix**) и $h_i$ – ее $i$-й диагональный элемент, называемый **левериджем (leverage)** наблюдения $x_i,i=\overline{1,n}$.

Леверидж $h_i$ показывает, насколько сильно предсказанное значение $\hat{y}_i$ зависит от наблюдаемого $y_i$. Он равен частной производной $\frac{\partial \hat{y}_i}{\partial y_i}$, что определяет влияние конкретного наблюдения на предсказанное значение при фиксированном значении регрессора $x_i$.

----
Стандартизованные остатки
----

В предположении о нормальности ошибок:
$$\hat\varepsilon_i\sim N(0,\sigma^2(1-h_i)),\hspace{5mm}i=\overline{1,n}$$

Поскольку дисперсия ошибок $\sigma$ неизвестна, используем ее несмещенную оценку $S_e$:
$$S_e^2=\frac{1}{n-k-1}\sum_{i=1}^{n}(y_i-\hat y_i)^2=\frac{1}{n-k-1}\sum_{i=1}^{n}(y_i-x_i\hat\beta)^2$$ 

Т.к. дисперсия остатков при фиксированном $x_i$ зависит от соответствующего левериджа, остатки необходимо стандартизовать, разделив на оценку стандартного отклонения. Это позволяет привести все остатки к единой шкале, устраняя влияние неоднородности их дисперсий.

**Стандартизованные остатки:**
$$t_i=\frac{\hat\varepsilon_i}{S[\hat\varepsilon_i]}=\frac{\hat\varepsilon_i}{S_e\sqrt{1-h_i}},\hspace{5mm}t_i\sim T(n-k-1),\hspace{5mm}i=\overline{1,n}$$

Значение $t_i$ (называемое также **стюдентизованным остатком**) может быть использовано как мера ~~удаленности $i$-го наблюдения от функции регрессии~~.

Использование метода взвешенных наименьших квадратов для робастной регрессии состоит в присвоении больших весов наблюдениям, близким к функции регрессии, и малых весов наблюдениям, удаленным от функции регрессии.

----
Iteratively Reweighted Least-Squares (IRLS) 
----

- **Шаг 1.** Установить единичную матрицу весов, $W=I$.
- **Шаг 2.** Построить WLS-модель.
- **Шаг 3.** Рассчитать стандартизованные остатки $t_1,...,t_n$ или робастные стандартизованные остатки $t_1',...,t_n'$: 
$$t'_i=\frac{\hat\varepsilon_i}{S'_e\sqrt{1-h_i}}, \quad i=\overline{1,n}$$
где $S'_e=\frac{med(|\hat\varepsilon_1|,...,|\hat\varepsilon_n|)}{0.675}$ -- **робастное с.к.о.**

- **Шаг 4.** Рассчитать робастные веса на основе $t'$.

**Биквадратные веса (bisquare weights)**:
	$$
	w_i=
	\begin{cases}
	(1-(t_i'/c_B)^2)^2,\hspace{5mm}|t_i'<c_B|,
	\\
	0,\hspace{5mm} otherwise
	\end{cases}
	\hspace{15mm}(c_B=4.685)
	$$

Пояснения:

- Если $t_i'$ превышает установленный порог (например, 4.685), наблюдению присваивается вес $0$, так как оно считается явным выбросом.
- Если $t_i'$ близок к нулю, вес стремится к $1$.
- Функция весов убывает по мере увеличения $t_i'$, уменьшая влияние наблюдений с большими стандартизованными остатками, т.е. наблюдений, удаленных от функции регрессии.

- **Шаг 5.** Перейти к шагу 2 (с учетом новых весов).

Процедура стандартизации остатков и обновления весов повторяется до тех пор, пока не выполнится критерий останова (например, по количеству итераций или по изменению коэффициентов модели). В результате получается робастная регрессия, устойчивая к выбросам, но основанная на квадратичном критерии без необходимости использования градиентных методов оптимизации.

📋 **Пример.**

На приведенной иллюстрации регрессионная линия, полученная стандартным методом OLS, представлена сплошной линией. Ее положение определяется влиянием выбросов: часть данных смещает линию вверх, другая часть — вниз.<br>

После нескольких итераций, при использовании робастного подхода, регрессионная модель становится менее чувствительной к выбросам. Веса выбросов снижаются, и итоговая линия регрессии (штрихованная линия) проходит через основную массу данных, минимизируя влияние аномальных точек.

@img03(robust1.png, Bisquare Weight Regression, 40)

📋 **Пример.** Cравнение различных методов регрессии.

Данные расположены вдоль некоторой линейной зависимости, при этом присутствуют выбросы.

- Черная линия соответствует методу OLS, который подвержен влиянию выбросов, что приводит к смещению оценок. В результате истинная функция регрессии искажена, а статистические свойства модели ухудшаются.
- Красная линия представляет LAR-регрессию, которая демонстрирует лучшую устойчивость к выбросам. 
- Синяя линия соответствует IRLS-регрессии, которая практически не реагирует на выбросы и проходит через основную массу данных, обеспечивая более точную аппроксимацию.

@img03(robust2.png, Робастные методы регрессии Иллюстрация, 40)

В большинстве случаев IRLS-регрессия с биквадратными весами предпочтительнее LAR-регрессии, поскольку одновременно с LS-обучением проводится минимизация влияния выбросов.

----
### Нелинейная регрессионная модель
----

Линейные регрессионные модели не могут описать все возможные зависимости. В некоторых задачах функция регрессии зависит от параметров $\beta$ нелинейным образом, и метод наименьших квадратов требует решения нелинейной системы уравнений. 

**Нелинейная регрессионная модель:**
$$Y|x=\varphi(x,\beta)+\varepsilon(x)$$
где $\beta=(\beta_0,...,\beta_k)^T$ – вектор параметров и $\varphi(x,\beta)$ – нелинейная функция от $\beta_0, ..., \beta_k$.

**Критерий:**
$$E(\beta)=\frac{1}{n}\sum_{i=1}^{n}(y_i-\varphi(x_i,\beta))^2\rightarrow\min_{\beta}$$

📋 **Примеры.**
$$\varphi(x,\beta)=\frac{\beta_0x}{\beta_1+x}$$
$$\varphi(x,\beta)=\beta_0+\beta_1x^{\beta_2}$$
$$\varphi(x,\beta)=\beta_0\sin(\beta_1+\beta_2x)$$

----
Нелинейный метод наименьших квадратов
----

Расчет частных производных критерия и решение нелинейных систем уравнений аналитически часто затруднительно, параметры $\beta_0,...,\beta_k$ ~~не всегда могут быть оценены в результате простых матричных вычислений~~. Поэтому для обучения нелинейных моделей применяют итеративные методы оптимизации, направленные на минимизацию суммы квадратов отклонений.

- **Шаг 1.** Задать начальное приближение для оценок обучаемых параметров $\beta_0,...,\beta_k$.
- **Шаг 2.** Вычислить модельную функцию регрессии $\varphi(x,\hat\beta)$ с текущими коэффициентами $\hat\beta$.
- **Шаг 3.** Скорректировать коэффициенты $\hat\beta$ в результате итерации некоторого ~~алгоритма оптимизации~~.
- **Шаг 4.** Перейти к шагу 2 до тех пор, пока не выполнится критерий останова.

В нелинейном методе наименьших квадратов класс параметрических нелинейных функций регрессии ~~должен быть задан априорно~~.

----
Преобразование к линейности
----

В некоторых случаях нелинейная модель может быть ~~преобразована к линейной~~ (линеаризирована).

📋 **Пример.**

**Нелинейная функция регрессии:** $$\varphi(x,\beta_0,\beta_1)=\beta_0 e^{\beta_1x}$$

**Преобразованная к линейной:** $$\ln\varphi(x,\beta_0,\beta_1)=\ln\beta_0+\beta_1x$$

$$\varphi'(x,\beta_0',\beta_1')=\beta_0'+\beta_1'x,$$

где $\varphi'(x,\beta_0',\beta_1')=\ln\varphi(x,\beta_0,\beta_1),\hspace{5mm}\beta_0'=\ln\beta_0,\hspace{5mm}\beta_1'=\beta_1$.

Это линейная модель, для оценки параметров $\beta'=(\beta_0',\beta_1')^T$ может быть применен OLS:
$$\beta'=(X^TX)^{-1}X^Ty',$$

где $y'=(\ln y_1,...,\ln y_n)^T$ и $X=\begin{pmatrix}1 & x_1\\ ... & ...\\ 1 & x_n \end{pmatrix}$.

Исходные параметры $\beta_0=e^{\beta_0'},\hspace{5mm}\beta_1=\beta_1'$.

При визуальном анализе можно заметить, что кривая, полученная в результате обучения линеаризированной модели (красная кривая на рисунке), недостаточно точно проходит через экспериментальные точки.

@img03(nonlin1.png, Преобразование к линейности 1, 40)

В то же время, минимизация суммы квадратов отклонений в исходном пространстве (в результате расчета частных производных критерия, их приравнивания нулю и решения полученной системы нелинейных уравнений), дает оценку параметров, более точно соответствующую данным (желтая кривая на рисунке).

@img03(nonlin2.png, Преобразование к линейности 2, 40)

Это происходит из-за того, что в логарифмическом пространстве некоторые точки становятся выбросами. В частности, значения $y$, близкие к нулю, при логарифмировании дают резко выделяющиеся точки, что искажает оценку параметров.

@img03(nonlin3.png, Преобразование к линейности 3, 40)

Даже после удаления выбросов оценки параметров, полученные разными методами, различаются. Это связано с тем, что минимизация ошибки в логарифмическом пространстве не эквивалентна минимизации ошибки в исходном пространстве.

@img03(nonlin4.png, Преобразование к линейности 4, 40)

----
Нелинейная регрессия или преобразование к линейности?
----

Для выбора правильного метода оценки параметров рассмотренной выше экспоненциальной модели необходимо проанализировать природу ошибок в данных. Рассмотрим два принципиально разных случая.

**Нелинейная модель 1:** $Y|x=\beta_0e^{\beta_1x}+\varepsilon(x)$ 
$$\ln Y|x=\ln(\beta_0e^{\beta_1x}+\varepsilon(x))$$

Если исходный шум $\varepsilon(x)$ ~~аддитивный~~, то логарифмическое преобразование неприемлемо, т.к. оно искажает структуру ошибок.

**Нелинейная модель 2:** $Y|x=\beta_0e^{\beta_1x}\varepsilon(x)$
$$\ln Y|x=\beta_0'+\beta_1'x+\varepsilon'(x)$$
где $\beta_0'=\ln\beta_0,\hspace{5mm}\beta_1'=\beta_1,\hspace{5mm}\varepsilon'(x)=\ln\varepsilon(x)$
$$\varepsilon'(x)\sim N(0,\sigma^2)\Leftrightarrow\varepsilon(x)\sim Lognormal(0,\sigma^2)$$

Если исходный шум $\varepsilon(x)$ ~~мультипликативный~~ и ~~логнормально распределенный~~, то логарифмическое преобразование приведет к линейной модели с аддитивным нормально распределенным шумом. Для оценки параметров этой модели может быть применен метод OLS.

📋 **Пример.**

@img03(nonlin5.png, Нелинейная регрессия или преобразование к линейности?, 60)

A) При аддитивных нормальных ошибках истинная модель (пунктирная линия) практически совпадает с моделью, полученной путем нелинейной оптимизации в исходном пространстве (красная линия).

B) При мультипликативных логнормальных ошибках истинная модель практически совпадает с моделью, полученной путем линеаризации (синяя линия).

@NB(`~~Важно.~~ Процедуры обучения нелинейной модели в исходном пространстве и линеаризованной модели в преобразованном пространстве неэквивалентны. Выбор подходящей процедуры обучения связан с видом предполагаемой статистической модели, описывающей данные.`)


----
Обобщенные линейные модели
----

Частный случай нелинейных моделей – **обобщенные линейные модели (Generalized Linear Models, GLM)**. 

В GLM отклик связан с линейной моделью через нелинейную **функцию связи (link function)**.

| Линейные модели   | Обобщенные линейные модели   |
| :--------- | :--------- |
| Для каждого $x$, отклик $Y |x$ распределен нормально     | Для каждого $x$, распределение отклика $Y |x$ может быть нормальным, биномиальным, Пуассона или пр.     |
| Параметры $\beta$ определяют линейную комбинацию $x\beta$     | Параметры $\beta$ определяют линейную комбинацию $x\beta$     |
| Функция регрессии линейная:<br>$M[Y|x] = x\beta$     | Преобразованная функция регрессии линейная:<br>$g(M[Y|x])=x\beta$,<br>где $g(\mu)$ – ~~функция связи~~|

----
Алгоритм построения GLM
----

Особенность GLM по сравнению с классическими линейными моделями заключается в необходимости дополнительного шага выбора функции связи, которая определяет характер нелинейной зависимости между линейной комбинацией предикторов и математическим ожиданием отклика. Этот выбор должен быть согласован с предполагаемым распределением зависимой переменной и природой исследуемых данных.

- **Шаг 1.** Подготовить данные.<br>
Определить регрессоры $x$ и отклик $y$.

- **Шаг 2.** Определить распределение отклика $Y|x$.<br>
Биномиальное, Пуассона, гамма или пр.

- **Шаг 3.** Выбрать функцию связи $f(\mu)$.<br>
Logit, probit, complementary log-log, log-log или др.
@img03(glm5.png, Пример GLM Выбор функции связи, 50)

- **Шаг 4.** Определить линейную модель.<br>
Наличие свободного члена, признаки.

- **Шаг 5.** Выбрать метод обучения.<br>
OLS, WLS или др.

- **Шаг 6.** Обучить модель.

- **Шаг 7.** Оценить качество обученной модели.<br>
Анализ остатков, проверка статистических гипотез, кросс-валидация.

----

📋 **Пример.** Моделирование зависимости доли автомобилей, не прошедших испытания, от их массы.

Регрессор $x$ – масса машины, отклик $y$ – доля машин различной массы, не прошедших испытание.

@img03(glm0.png, Пример GLM 1, 40)

Отклик $Y$ представляет собой долю в диапазоне $[0, 1]$. Наблюдается явная зависимость: с ростом массы процент отказов увеличивается.

**1. Простейшая линейная регрессия.**

Линейная модель: $\varphi(x)=\beta_0+\beta_1x$

@img03(glm1.png, Пример GLM 2, 40)

**Проблемы:**

- Линейная функция неограниченна и может давать прогнозы меньше $0$ или больше $1$, что не соответствует природе данных.
- Распределение доли машин не нормально – оно ограничено в интервале. Это нарушает одно из предположений, требуемых при OLS-обучении простой линейной регрессионной модели.

**2. Полиномиальная регрессия.**

Линейная модель: $\varphi(x)=\beta_0+\beta_1x+\beta_2x^2+\beta_3x^3$

@img03(glm2.png, Пример GLM 3, 40)

**Проблемы:**

- Модельная доля начинает уменьшаться при массе более 4000; для больших масс она станет отрицательной.
- Предположение о нормальности по-прежнему нарушено.

**3. Обобщенная линейная модель (GLM).**

Разумно предположить, что число машин, не прошедших испытание, имеет ~~биномиальное распределение~~ с параметром $p$, который увеличивается с увеличением массы.

Так, распределение $Y|x$ должно быть биномиальным (с точностью до множителя).

Построим GLM $g(\varphi(x))=\beta_0+\beta_1x$ с ~~логит-функцией связи~~
$$g(\mu)=\ln\left(\frac{\mu}{1-\mu}\right),\hspace{5mm}0<\mu<1$$

Логит-функция ограничивает модельные значения отклика в интервале $(0, 1)$, что соответствует задаче.

Обратная логит-функция -- ~~логистическая~~ $g^{-1}(z)=\frac{1}{1+e^{-z}}$.

**Функция регрессии:**
$$\varphi(x)=g^{-1}(\beta_0+\beta_1x)=\frac{1}{1+e^{-(\beta_0+\beta_1x)}}$$

GLM: $g(\varphi(x))=\beta_0+\beta_1x$, где $g(\mu)=\ln(\frac{\mu}{1-\mu})$ и $Y|x$ имеет биномиальное распределение.

@img03(glm3.png, Пример GLM 4, 40)

Модельные доли асимптотически стремятся к $0$ или $1$ при уменьшении или увеличении массы.

~~Замечание.~~ Для биномиального распределения отклика логит-функция является канонической связью. Она обеспечивает оптимальные статистические свойства оценок параметров регрессии.

----
Нейросетевая регрессия
----

Еще один частный случай нелинейной регрессии -- **нейросетевая регрессионная модель**:
$$Y|x=\varphi(x,\beta)+\varepsilon(x)$$
где $\beta=(\beta_0,...,\beta_k)^T$ -- вектор параметров и $\varphi(x,\beta)$ – ~~нейросетевая функция~~, нелинейная по $x$ и по $\beta_1,...,\beta_k$.

Функция $\varphi(x,\beta)$ имеет специальный вид, это сложная композиция простых нелинейностей (логистических функций, гауссиан и т.п.).

Теоретически нейронная сеть способна аппроксимировать любую непрерывную функцию.
Требуемая сложность сети зависит от сложности моделируемой зависимости.

Особенности нейросетевой регрессии:

- Высокая гибкость модели.
- Число оцениваемых параметров $k$ может быть ~~очень большим~~, достигать тысяч или миллионов. Оно растет с числом нейронов, количеством слоев и размерностью входа.
- Для обучения требуется большой объем данных.
- Сложность интерпретации параметров.
- Вычислительная затратность обучения.

**OLS-критерий:**
$$E(\beta)=\frac{1}{n}\sum_{i=1}^n(y_i-\varphi(x_i,\beta))^2\rightarrow\min_{\beta}$$

**Модель нейрона:**
$$y=f\left(\sum_{j=1}^Mw_jx_j\right),$$
где $x_1,...,x_M$ – входы нейрона,<br>
$y$ – выход нейрона,<br>
$f(·)$ – функция активации.

📋 **Пример.** Двухслойная нейронная сеть.

@img03(neuron.png, Нейросетевая регрессия Пример, 50)

Скрытый слой: нейроны $1, 2, 3$.

Выходной слой: нейрон $4$.

**Функция регрессии:**
$$\varphi(x)=f_4(w_4f_1(w_1x)+w_5f_2(w_2x)+w_6f_3(w_3x)),$$
где $f_i$ -- функция активации $i$-го нейрона, $i=1,...,4$.

**OLS-критерий обучения:**
$$E(w_1,...,w_6)=\frac{1}{n}\sum_{i=1}^n(y_i-\varphi(x_i,w_1,...,w_6))^2\rightarrow\min_{w_1,...,w_6}$$

----
### ❓ Вопросы для самопроверки
----

1. Какие типы моделей получаются в результате model-based и data-driven  подходов?
2. Почему в статистических моделях важно задавать класс распределений?
3. Что такое функция регрессии?
4. Какие три основные цели регрессионного анализа?
5. Опишите виды регрессионного анализа: параметрический, полупараметрический и непараметрический.
6. Как выводится решение OLS для простой линейной регрессии?
7. Какие предположения лежат в основе OLS? Что произойдёт, если они нарушатся?
8. Почему оценки OLS считаются наилучшими в классе линейных несмещённых оценок?
9. В чем отличие (случайных) регрессионных ошибок и (случайных) регрессионных остатков?
10. В чем отличие множественной линейной регрессии от простейшей? Как применять OLS для множественной линейной регрессии?
11. Как проверить гипотезу о значимости коэффициента регрессии?
12. Чем доверительный интервал для условного матожидания $φ(x)$ отличается от интервала предсказания для $Y|x$?
13. Почему интервал предсказания всегда шире доверительного интервала?
14. Что такое гетероскедастичность? Как её обнаружить и устранить?
15. Какие методы регрессии устойчивы к выбросам?
16. В чем заключается метод взвешенных наименьших квадратов WLS? Чем он отличается от обобщенного метода наименьших квадратов GLS?
17. Какие есть способы линеаризации нелинейных моделей? Когда это допустимо?
18. Что можно сделать, если нормальность ошибок нарушена? Какие робастные регрессионные модели вы знаете?
19. Какие есть виды нелинейных регрессионных моделей? 
20. Как выбрать между нелинейной оптимизацией в исходном пространстве и преобразованием к линейности?

----
### ✅ Тест по разделу
----

@quizQuestion(`Какое условие нарушается, если дисперсия ошибок регрессии зависит от значений регрессоров?`)

<!-- data-randomize -->
- [(x)] Гомоскедастичность
- [( )] Независимость ошибок
- [( )] Нормальность распределения ошибок
- [( )] Линейность модели

@quizQuestion(`Определите какое из предположений регрессионного анализа нарушается на каждом из следующих графиков:`)

<!-- data-randomize -->
- [(Нарушение линейности) (Гетероскедастичность) (Нарушение нормальности распределения ошибок) (Корреляция ошибок)]
- [ (x) ( ) ( ) ( )]  @img03(gr1.png, Нарушение линейности, 100)
- [ ( ) (x) ( ) ( )]  @img03(gr2.png, Гетероскедастичность, 100)
- [ ( ) ( ) (x) ( )]  @img03(gr3.png, Нарушение нормальности, 100)
- [ ( ) ( ) ( ) (x)]  @img03(gr4.png, Корреляция ошибок, 100)

@quizQuestion(`Определите к каким ухудшениям свойств OLS-оценок приводят нарушения предположений регрессионного анализа:`)

<!-- data-randomize -->
- [(Гетероскедастичность) (Нелинейность) (Автокорреляция) (Экзогенность регрессоров)]
- [ (x) (x) (x) (x)]  Теряется эффективность
- [ ( ) (x) ( ) ( )]  Оценки становятся смещенными
- [ ( ) (x) ( ) (x)]  Нарушается состоятельность

@quizQuestion(`Почему в методе наименьших квадратов (OLS) используют квадраты отклонений, а не модули?`)

<!-- data-randomize -->
- [(x)] Квадратичная функция дифференцируема, что упрощает оптимизацию
- [( )] Модули дают более точные результаты при любом распределении ошибок
- [(x)] Позволяет получить аналитическое решение через систему линейных уравнений
- [(x)] Квадратичная функция выпукла, что гарантирует единственный минимум

@quizQuestion(`Какой метод регрессии наиболее устойчив к выбросам?`)

<!-- data-randomize -->
- [( )] OLS (обычный МНК)
- [(x)] LAR (метод наименьших модулей)
- [( )] WLS с весами, обратными дисперсиям
- [(x)] IRLS с биквадратными весами

@quizQuestion(`Какое преобразование данных может устранить гетероскедастичность?`)

<!-- data-randomize -->
- [[x]] Логарифмирование отклика $Y$
- [[ ]] Умножение всех признаков на константу
- [[x]] Использование WLS с весами, обратными дисперсиям
- [[ ]] Добавление квадратичного члена в регрессию


@quizQuestion(`Когда целесообразно применение логарифмического преобразования к нелинейной модели?`)

<!-- data-randomize -->
- [( )] Всегда, так как это упрощает расчёты
- [(x)] Если ошибки мультипликативные и логнормально распределены
- [( )] Если ошибки аддитивные и нормально распределены
- [( )] Когда функция связи соответствует природе данных

@quizQuestion(`Сопоставьте метрики с формулами их расчета:`)

<!-- data-randomize -->
- [(Регрессионные ошибки) (Случайные регрессионные ошибки) (Регрессионные остатки) (Случайные регрессионные остатки)]
- [ (x) ( ) ( ) ( )]  $\varepsilon_i=y_i-φ(x_i)$
- [ ( ) (x) ( ) ( )]  $\varepsilon_i=Y_i-φ(x_i)$
- [ ( ) ( ) (x) ( )]  $\hat \varepsilon_i=y_i-\hatφ(x_i)$
- [ ( ) ( ) ( ) (x)]  $\hat \varepsilon_i=Y_i-\hatφ(x_i)$

@quizQuestion(`Сформулируйте теорему Гаусса-Маркова.`)

Из всех линейных несмещенных оценок OLS-оценки являются наиболее [[эффективными]], т.е. они имеют [[наименьшую]] дисперсию. Более того, в предположении о [[нормальности]] они являются наиболее [[эффективными]] среди всех [[несмещенных оценок]].

@quizQuestion(`Отметьте предположения линейного регрессионного анализа, нарушение которых может привести к нелинейности, смещенности, неэффективности или несостоятельности OLS-оценок.`)

<!-- data-randomize -->
- [[x]] Линейность
- [[x]] Экзогенность регрессоров
- [[x]] Гомоскедастичность
- [[x]] Независимость ошибок
- [[x]] Нормальность
- [[x]] Вариабельность регрессоров

@quizQuestion(`Какие предположения должны выполняться, чтобы МНК имел строгое статистическое обоснование?`)

<!-- data-randomize -->
- [(x)] линейность
- [(x)] гомоскедастичность
- [(x)] независимость ошибок
- [(x)] нормальность ошибок
- [(x)] экзогенность регрессоров

@quizQuestion(`Выберите все верные высказывания.`)

<!-- data-randomize -->
- [[x]] Доверительный интервал определяет интервал значений для неизвестного условного мат. ожидания $M[Y |x]$
- [[ ]] Интервал предсказания определяет интервал значений для неизвестного условного мат. ожидания $M[Y |x]$
- [[x]] Интервал предсказания определяет интервал, в котором будут находиться наблюдения СВ $Y |x$
- [[x]] Интервал предсказания всегда шире, чем соответствующий доверительный интервал
